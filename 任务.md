# 当前任务清单（截至 2026-04-19 夜；W3 已冻结）

## 已完成

- [x] 核对当前语义聚类的真实决策路径
  - 结论：当前事件合并的 merge graph 主要由 cosine 相似度 + 自适应阈值驱动。
  - `SEMANTIC_W_TIME` 和 `SEMANTIC_W_PLATFORM` 目前不会直接决定 Union-Find 合并边。

- [x] 补全评测脚本 `scripts/evaluate_clustering.py`
  - 已支持 pair 评测：`persisted` / `jaccard` / `semantic`
  - 已支持 gold cluster 导入与 `ARI / NMI / V-measure`
  - 已支持 `--systems`
  - 已支持 `--restrict-to-pair-articles`
  - 已支持 `--semantic-existing-embeddings-only`
  - 已支持 `--export-gold-template`

- [x] 生成闭集标注所需文件
  - `runtime/eval/gold_clusters_seed_100_template.csv`
  - `runtime/eval/gold_clusters_example.csv`
  - `runtime/eval/gold_clusters_format.md`

- [x] 跑通 pair 级评测
  - Persisted Events：`Precision=0.9000`，`Recall=0.2308`，`Accuracy=0.6593`，`F1=0.3673`
  - Jaccard Baseline：`Precision=0.9091`，`Recall=0.2564`，`Accuracy=0.6703`，`F1=0.4000`
  - Semantic Clustering：`Precision=0.8438`，`Recall=0.6923`，`Accuracy=0.8132`，`F1=0.7606`

- [x] 验证 cluster 指标计算流程
  - 已用示例 gold 文件跑通 `ARI / NMI / V-measure`
  - 当前这组示例结果只用于验证流程，不作为正式实验结论

- [x] 初审人工填写的 gold 标注文件
  - `gold_cluster_id` 使用数字编号是合法的
  - 已确认当前 gold 标注整体偏细，确实存在“合并偏少”的情况
  - 已发现 pair 标签与 gold 标签之间存在口径冲突

- [x] 对齐一部分关键标注口径
  - 同一事件的不同进展/后续报道，默认应合并到同一个 `gold_cluster_id`
  - “韩国标注中国台湾”与“日本军舰进台湾海峡”可保留合并
  - “丹麦把台湾人国籍改中国”不并入上面那一簇
  - “何超蕸去世”与“何超蕸终身未婚”算同一事件
  - 伊朗相关当前分散在 `1 / 2 / 17` 的条目，按现口径应合并成一个大簇

## 2026-04-19 夜新增完成（W3 收尾）

- [x] 修正 pair 与 gold 的冲突样本
  - 声明式仲裁文件 `runtime/eval/reconciliation.json`（8 条 gold 簇合并 + 2 条 pair 标签翻转）
  - `scripts/apply_annotation_reconciliation.py` 做幂等应用
  - `scripts/verify_annotation_consistency.py` 做交叉校验
  - pre-apply 冲突=11 → post-apply 冲突=0（二次运行 0 变动，确认幂等）
  - 修前备份：`_backups/2026-04-19_pre-w3-reconciliation/`

- [x] 运行正式闭集评测
  - 原始 stdout：`runtime/eval/results/main_eval_final.txt`
  - 冻结数字（清理后 91 对 pair + 155 闭集 gold）：
    - Persisted：`P=0.9000`、`R=0.2308`、`F1=0.3673`、`ARI=0.3444`、`NMI=0.9587`
    - Jaccard：`P=1.0000`、`R=0.3077`、`F1=0.4706`、`ARI=0.3046`、`NMI=0.9612`
    - Semantic（主路径）：`P=0.8611`、`R=0.7949`、`F1=0.8267`、`ARI=0.6913`、`NMI=0.9774`
  - 相对 Jaccard baseline：**F1 +0.356（+76%），ARI +0.387（+127%），Recall 2.58 ×**

- [x] 实现可用的消融入口
  - 新增函数签名级开关（不污染全局 state，对生产路径零影响）：
    - `fixed_threshold`：旁路双层 Otsu 的 L2，用固定值代替
    - `skip_verification`：跳过簇内复核
    - `disable_canonical_merge`：跳过 canonical-title 硬合并边
  - `evaluate_clustering.py --ablation all|<name ...>` 一键跑所有预设

- [x] 做有科研意义的消融实验（保守 3 项）
  - 原始 stdout：`runtime/eval/results/ablation_final.txt`
  - 发现：
    - 固定阈值 0.62 → F1 −0.027、ARI −0.022（自适应阈值有效）
    - canonical-title 硬边去掉 → F1 −0.060、ARI −0.048（混合符号规则确有收益）
    - 簇内复核去掉 → F1 +0.005、ARI +0.017（诚实报告：复核在本 seed 下略保守，值得 Discussion 提及）

- [x] 形成论文/答辩可直接使用的结果表
  - `runtime/eval/results/final_tables.md`：Table 1（主表）+ Table 2（消融）+ Limitations 段，可直接粘贴到论文与 slide。

- [x] 修复独立 bug：`evaluate_clustering.py::_load_gold_clusters` 只认 `cluster_id` 字段，导致 gold 模板（`gold_cluster_id`）被当成空，ARI/NMI/V-measure 永远打不出。现已兼容两个字段名。

## 仍未完成（已降级为"下一阶段候选"）

- [ ] 如果坚持做时间/平台消融，需要先修改 `app/services/semantic_index.py::_prepare_semantic_materials`
  - 让 composite（含时间衰减、平台多样性）真正进入 pair graph 的合并判定
  - 然后再对 `SEMANTIC_W_TIME` / `SEMANTIC_W_PLATFORM` 做加权消融
  - W3 明确不做；本条保留作为论文可扩展实验的预留位

- [ ] 扩大标注集规模（500+ pair）以进一步稳定 ARI/NMI
  - 可沿用 `scripts/generate_eval_pairs.py` 的候选挖掘，再人工抽检
  - 本 W3 范围不做

## 后续建议

- 进入 **W4：LLM Tool-Calling Agent**（答辩爆点）或 **W5：UI / 性能 P0**（稳定性）
- 论文主表 + 消融表均已冻结并 commit，答辩 slide 可以开始铺版