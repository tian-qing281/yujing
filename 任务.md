# 当前任务 · 2026-04-19 夜起 · W4 (LLM Tool-Calling Agent)

> **总路线**见 `yujing-phase2-plan.md`；本文件只列 **W4 可执行 backlog**。
> W3 已冻结的完整清单归档在本文末"历史：W3 完成清单"区域。

**进度总览**

| 里程碑 | 状态 | 完成日 | 关键指标 |
|:---|:---:|:---:|:---|
| M1 引擎骨架 | ✅ | 2026-04-19 | 6 单测 / 2ms · commit `efd3a3a` |
| M2 八个工具 | ✅ | 2026-04-19 | 32 单测 / 1.6s · real-DB smoke 通过 · commits `57f7f04` + `61a5c78` |
| M3 `/api/agent/chat` SSE | ✅ | 2026-04-19 | 5 集成测试 / 280ms · real-DB + FakeLLM smoke 通过 |
| M4 前端 AgentTrace | ⏳ | — | 估 1-2 天 |
| M5 评测 + 答辩脚本 | ⏳ | — | 估 1 天 |
| M6 文档同步 + 冻结 | ⏳ | — | 估 0.5 天 |

---

## W4 · LLM Tool-Calling Agent（进行中）

**目标**：live demo 场景——用户用自然语言提问 → Agent 自动编排多工具调用 → 带来源引用的分析结论。

**完成判据**（三条全绿即为 W4 结束）：
1. 20 个测试 query 的 `task_completion_rate ≥ 0.80`
2. 平均 step 数 ≤ 4，p95 latency ≤ 8s
3. 前端演示 3 个预设复杂 query 零崩溃

### M1 · 引擎骨架 ✅（已完成 · commit `efd3a3a`）

- [x] 新建 `app/services/agent/` 包
  - [x] `schemas.py`：`ToolSpec` / `ToolCall` / `ToolResult` / `AgentStep` / `AgentTrajectory` 数据类 + `to_dict()`
  - [x] `registry.py`：`ToolRegistry` 单例 + `default_registry` + OpenAI function 格式导出
  - [x] `loop.py`：`AgentLoop.run(query)` 主循环 · `MAX_STEPS=6` · `MAX_CONSECUTIVE_ERRORS=2`
  - [x] `llm_adapter.py`：LangChain `ChatOpenAI.bind_tools` 封装 · 复用 `app/llm.py` 的 DeepSeek settings
- [x] 硬上限保护：step ≤ 6（超限 `terminated_reason="max_steps"`）· 连续错误 ≤ 2（超限 `"too_many_errors"`）
- [x] 非法 JSON / 未注册工具名 / handler 抛错：全部变成 `ToolResult(error=...)` observation 让 LLM 自我修复
- [x] 单测：`app/tests/test_agent_loop.py` · 6 tests · FakeLLM 注入完全离线
  - `three_step_tool_chain_then_final`（正路径）
  - `unknown_tool_becomes_observation_error_then_recovers`
  - `handler_exception_wrapped_as_error`
  - `max_steps_terminates_gracefully`
  - `llm_call_exception_captured`
  - `trajectory_to_dict_is_json_serializable`

**M1 设计亮点**：
- 同步 API（流式移到 M3 的 API 层，保持 Loop 简单）
- `llm_caller` 可注入，单测完全不碰 DeepSeek 凭证
- 不用 `langchain.agents.AgentExecutor`（自控循环便于 step 可视化、评测、复盘）

### M2 · 工具实现 ✅（已完成 · commits `57f7f04` + `61a5c78`）

全部在 `app/services/agent/tools/` 下，每个工具一个文件，模块级 `default_registry.register(SPEC)` 自注册。bootstrap 通过 `from app.services.agent.tools import *` 触发。

#### 第一批 · 简单工具（`57f7f04`）
- [x] `tool_search_events.py` · 参数：`q / time_range_hours / source_id / limit` · 复用 `services.events.search_events`
- [x] `tool_get_event_detail.py` · 参数：`event_id / top_articles` · 只读不触发 BERT 补全
- [x] `tool_get_morning_brief.py` · 参数：无 · **只读 cache 不触发生成**（避免 30s hang）· cache miss 返回 hint
- [x] `tool_list_hot_platforms.py` · 参数：`time_range_hours / top_events_per_platform` · 按 source_id group by + per-platform top events

#### 第二批 · 进阶工具（`61a5c78`）
- [x] `tool_search_articles.py` · 参数：`q / time_range_hours / source_id / limit` · Meili 优先 · 失败/空自动降级 DB LIKE
- [x] `tool_semantic_search_articles.py` · 参数：`q / limit / source_id` · 双阶段 Meili seed → BGE kNN · 3 条独立 hint 路径
- [x] `tool_analyze_event_sentiment.py` · 参数：`event_id / bucket_hours∈{6,12,24}` · 分布 Counter + 时间桶时序
- [x] `tool_compare_events.py` · 参数：`event_ids` (2-4，支持 `list` / JSON 字符串 / int) · 关键词交并集 + max heat 摘要

#### 通用约定
- [x] 每个 item 输出带 `_id / _type / _title` 元字段（便于 final answer 中 `event#N` / `article#N` 引用）
- [x] 延迟 import `SessionLocal` / `meili`（避免启动期 DB 连接 + 单测 `patch('app.database.SessionLocal')` 从源头）
- [x] 输入超范围不拒绝，自动 clamp（LLM 偶尔越界不应浪费一整步自修复）
- [x] 所有 handler 抛异常由 Loop 捕获成 `ToolResult.error`

#### 测试 · 32/32 绿 · 1.6s
- `app/tests/test_agent_tools.py` · 8 个 TestCase 类
- 全部 mock `SessionLocal` / `meili` / `semantic_index` 源头，完全离线
- Real DB smoke test（5500+ events）跑通所有 8 个工具

#### M2 顺手解决的历史债
- [x] `app/tests/test_semantic_index.py` 引用已改名函数（W3 双层 Otsu 重构遗留）· commit `2e671bd`

### M3 · API 串通 ✅（已完成 · 2026-04-19）

- [x] `POST /api/agent/chat` 接口
  - 入参：`{ "message": str, "stream": bool=true, "max_steps": int? }`
  - 出参 SSE 流（每条 `data: <json>\n\n`）：
    - `{"type":"llm_thinking","step":i}` — 每次 LLM 调用前
    - `{"type":"tool_call","step":i,"name":"...","args":{...}}`
    - `{"type":"tool_result","step":i,"name":"...","ok":bool,"output":...,"error":null,"latency_ms":int}`
    - `{"type":"final","text":"..."}` — 最终答案
    - `{"type":"error","message":"...","terminated_reason":"..."}` — 致命错误
    - `{"type":"done","terminated_reason":"final|max_steps|too_many_errors|error","total_latency_ms":int}` — 结束
- [x] 非流式 fallback（`stream=false`）：`{ "finished": bool, "terminated_reason": str, "total_latency_ms": int, "final": str, "trajectory": {...} }`
- [x] `GET /api/agent/tools` — 暴露已注册工具 schema，便于前端和调试
- [x] `AgentLoop.run(query, on_event=...)` 加回调 hook，不破坏现有同步 API
- [x] 流式实现：`asyncio.to_thread(loop.run, ...)` + `asyncio.Queue` 跨线程桥接 + `call_soon_threadsafe` 避免竞争
- [x] Tools 包 lazy bootstrap（`_ensure_tools_registered` 幂等），避免启动阶段副作用
- [x] 集成测试 `app/tests/test_agent_api.py` · 5 tests · TestClient + FakeLLM
  - `happy_path_returns_full_trajectory`（非流，3 步链路）
  - `empty_message_returns_422`
  - `tool_error_recovered_by_llm`（未知工具 → error observation → LLM 换工具收敛）
  - `streaming_emits_expected_event_types`（SSE 事件顺序）
  - `list_tools_returns_registered_specs`
- [x] Real-DB smoke test（TestClient + FakeLLM + 真 8 工具 + 真 SQLite）：
  - Blocking 模式：200 OK · finished=True · 1226ms · 3 步真 tool chain
  - Streaming 模式：12 SSE events（4 llm_thinking + 3 tool_call + 3 tool_result + 1 final + 1 done）

**M3 设计决策**：
- 流式放在 API 层，不改 Loop 内部的同步/可测试性
- 未写 `agent_trajectories` 落库表（M5 评测阶段再加，MVP 不需要持久化）
- 未实现多轮对话（`conversation_id` 只占位，保持 MVP 单轮专注）

#### 真 DeepSeek 端到端验收 ✅（2026-04-19 晚）

Query：**"最近两周伊朗相关的舆情消息中，哪些事件最热？情绪是什么倾向？"**

| 指标 | 值 |
|:---|:---|
| 端到端耗时 | 47.17s（4 次 DeepSeek 调用 ≈ 10s/次 + 4 次工具执行 ≈ 1.6s）|
| SSE 事件数 | 15（4 llm_thinking + 4 tool_call + 4 tool_result + 1 final + 2 含 done） |
| Agent 自主选工具 | 4 个：`search_events` → `get_event_detail` → `analyze_event_sentiment` × 2 |
| 步数 | 4 步（符合 ≤ 4 目标） |
| Final 引用 | `event#1` / `event#2938` / `article#11965` / `article#11856`（均真实存在） |
| 数据准确性 | 热度 410.95、70 篇、8 平台、情绪占比 47.8%/14.5%/11.6%/... 全部可 DB 核对 |

**答辩素材**（可直接进论文 LLM Agent 实证章节）：
- Agent 准确理解"最近两周"→ `time_range_hours=336`
- Agent 主动挑选热度最高的 event#1 深入分析，而不是对所有 8 个事件依次分析（节省 step）
- Final answer 结构化良好：事实 → 情绪特征 → 时间演化 → 平台分布 → 研判
- 零幻觉：所有引用的 event_id 和 article_id 都可在 DB 中核对

**性能短板**：47s 超过 8s p95 目标。主要瓶颈是 4 次 DeepSeek 串行调用。可选优化：
- M4：前端流式展示 tool_result，用户感知首个结果在 2s 内
- M5：对独立工具调用并行化（如 2 次 `analyze_event_sentiment` 可并发）

### M4 · 前端集成（1-2 天）

- [ ] 新建 `yujing-ui/src/components/AgentTrace.vue`：SSE 驱动的调用链卡片列表
  - 每个 step 显示：图标 · 工具名 · 参数摘要 · 耗时 · result 前 200 字
  - 折叠展开，默认显示前 3 步 + 最后 1 步
- [ ] 改造 `AIConsultant.vue`：在消息气泡下方挂 `<AgentTrace>`，点击展开
- [ ] 引用高亮：final answer 中 `event#N` / `article#N` 自动变蓝色可点击，点开 `EventModal`
- [ ] 3 个预设 query 按钮（便于评委挑一个点）：
  - "生成今日舆情早报 + 对比微博与头条热度走势"
  - "最近三周伊朗局势怎么演化？情绪倾向有转折吗？"
  - "苹果发布会后的舆情情绪 vs 华为 Mate70 发布会对比"

### M5 · 评测 + 答辩脚本（1 天）

- [ ] 新建 `runtime/eval/agent_queries.csv`：20 query · 5 类各 4 条
- [ ] `scripts/evaluate_agent.py`：跑全集、人工标注 `final_correct` / `tool_precision` / `tool_recall` 三列、输出 markdown 表
- [ ] `runtime/eval/agent_eval_final.md`：最终指标表 + 5 个典型 case 复盘
- [ ] `runtime/docs/demo_script_w4.md`：3 分钟答辩 live 脚本（含 fallback："如果 Agent 卡了，按下此键重新起 query"）

### M6 · 冻结 + commit + 文档同步（0.5 天）

- [ ] git commit 分阶段：每个 M 一个 commit，message 含验收数据
- [ ] 更新 `项目文档.md` 新增 "LLM Agent 架构" 小节
- [ ] 更新 `部署文档.md`：新增 `agent_trajectories` 表、`/api/agent/chat` 接口说明
- [ ] 更新 `yujing-phase2-plan.md` 把 W4 状态从 🟡 改为 ✅

---

## W4 非目标（明确不做 · 避免范围蔓延）

- ❌ 多轮对话记忆（M6 后再评估，MVP 单轮 Agent）
- ❌ 工具生成新工具（meta-agent）
- ❌ 向量库持久化 trajectory 做知识图谱
- ❌ 迁移到 LangChain / LlamaIndex（自实现就够）
- ❌ WebSocket（SSE 够用）
- ❌ 鉴权 / 限流 / 配额

---

## 风险监控（每日 standup 过一遍）

| 风险 | 触发条件 | 预案 | 状态 |
|:---|:---|:---|:---:|
| DeepSeek 限流 | 5 分钟内 429 ≥ 3 次 | 切 `/chat/completions` 非 function 模式，手工 parse | 🟢 未触发 |
| 工具调用死循环 | 单 query step = 6 但无 final | 强制返回 "超步数" 友好降级 | 🟢 M1 已实现 |
| 前端 SSE 卡顿 | 用户感知 > 2s 无响应 | 加"Agent 正在思考…"骨架屏 | ⏳ M4 待做 |
| 论文评测集太小 | 20 query 无法稳定体现差异 | 加 seed 变体 = 60 query（每 query 改 1-2 词） | ⏳ M5 关注 |

### 非 W4 核心的已知问题（smoke test 发现，不阻塞 W4）

| 问题 | 影响 | 现状 | 下一步 |
|:---|:---|:---|:---|
| **MeiliSearch `typoTolerance` 版本不兼容** | `meili.search_articles/events` 每次都抛 400；events.py 里被吞了异常并回退 DB 直查；`search_articles` 工具已内建 DB LIKE fallback | 工具不崩，但召回率变差（substring vs 语义 tokenization） | W5 修，改 `services/search_engine.py::search` 里的 `typoTolerance` 字段结构或移除。估 0.2 天 |
| **语义索引未自动 build** | Agent 启动后 `_semantic_index_state.ready` 默认 False，`tool_semantic_search_articles` 一直走 hint 分支 | 工具返回"改调 search_articles"提示，不崩 | W5 做：启动钩子 + 后台异步 build，或管理端按钮。估 0.5 天 |

---

## 下一阶段（W5+）预告

- **W5**：UI 三件套（暗色主题 / EventModal 重做 / 通知中心）+ 性能 P0（chunk 拆分 / 持久化 / BERT 批处理）
- **W6**：Demo 视频 · 答辩 slide · 论文初稿 · 回归测试

---

## 历史：W3 完成清单（2026-04-19 夜 冻结）

### 算法 / 工程
- [x] 评测脚本 `scripts/evaluate_clustering.py` 完整实现（pair 评测 / gold cluster / `--systems` / `--restrict-to-pair-articles` / `--ablation` / `--export-gold-template`）
- [x] 闭集标注：91 pair + 155 article gold
- [x] 声明式仲裁：`reconciliation.json` + apply + verify 三件套（pre 11 冲突 → post 0 冲突 · 二次运行 0 变动）
- [x] 消融 3 开关：`fixed_threshold` / `skip_verification` / `disable_canonical_merge`
- [x] 论文表：`runtime/eval/results/final_tables.md`（Table 1 主表 + Table 2 消融 + Limitations）

### Bug 修
- [x] `_load_gold_clusters` 只认 `cluster_id` 导致 `gold_cluster_id` 字段被忽略 → 已兼容两字段
- [x] 同 canonical 文章被拆成多个独立 event（"伊朗总统感谢中国" ×3）→ canonical 精确救回（commit `7f6d7d9`）

### 关键指标（冻结）
- Persisted：P=0.900 · R=0.231 · F1=0.367 · ARI=0.344 · NMI=0.959
- Jaccard：P=1.000 · R=0.308 · F1=0.471 · ARI=0.305 · NMI=0.961
- **Semantic**：**P=0.861 · R=0.795 · F1=0.827 · ARI=0.691 · NMI=0.977**
- 相对 Jaccard：F1 +76% · ARI +127% · Recall 2.58×

### 标注对齐（W3 共识 · 可以删但保留作论文附录来源）
- 同一事件的不同进展 / 后续报道默认合并同一 `gold_cluster_id`
- "韩国标注中国台湾" + "日本军舰进台湾海峡"合并
- "丹麦把台湾人国籍改中国"不并入上述簇
- "何超蕸去世" + "何超蕸终身未婚"同一事件
- 伊朗相关三个散 cluster (id=1/2/17) 合并

### 已降级为"下一阶段候选"
- [ ] 时间 / 平台消融（需先改 `_prepare_semantic_materials` 让 composite 进入合并判定，W4 不做）
- [ ] 标注集扩大到 500+ pair（W4 不做）
