# 当前任务清单（截至 2026-04-19）

## 已完成

- [x] 核对当前语义聚类的真实决策路径
  - 结论：当前事件合并的 merge graph 主要由 cosine 相似度 + 自适应阈值驱动。
  - `SEMANTIC_W_TIME` 和 `SEMANTIC_W_PLATFORM` 目前不会直接决定 Union-Find 合并边。

- [x] 补全评测脚本 `scripts/evaluate_clustering.py`
  - 已支持 pair 评测：`persisted` / `jaccard` / `semantic`
  - 已支持 gold cluster 导入与 `ARI / NMI / V-measure`
  - 已支持 `--systems`
  - 已支持 `--restrict-to-pair-articles`
  - 已支持 `--semantic-existing-embeddings-only`
  - 已支持 `--export-gold-template`

- [x] 生成闭集标注所需文件
  - `runtime/eval/gold_clusters_seed_100_template.csv`
  - `runtime/eval/gold_clusters_example.csv`
  - `runtime/eval/gold_clusters_format.md`

- [x] 跑通 pair 级评测
  - Persisted Events：`Precision=0.9000`，`Recall=0.2308`，`Accuracy=0.6593`，`F1=0.3673`
  - Jaccard Baseline：`Precision=0.9091`，`Recall=0.2564`，`Accuracy=0.6703`，`F1=0.4000`
  - Semantic Clustering：`Precision=0.8438`，`Recall=0.6923`，`Accuracy=0.8132`，`F1=0.7606`

- [x] 验证 cluster 指标计算流程
  - 已用示例 gold 文件跑通 `ARI / NMI / V-measure`
  - 当前这组示例结果只用于验证流程，不作为正式实验结论

- [x] 初审人工填写的 gold 标注文件
  - `gold_cluster_id` 使用数字编号是合法的
  - 已确认当前 gold 标注整体偏细，确实存在“合并偏少”的情况
  - 已发现 pair 标签与 gold 标签之间存在口径冲突

- [x] 对齐一部分关键标注口径
  - 同一事件的不同进展/后续报道，默认应合并到同一个 `gold_cluster_id`
  - “韩国标注中国台湾”与“日本军舰进台湾海峡”可保留合并
  - “丹麦把台湾人国籍改中国”不并入上面那一簇
  - “何超蕸去世”与“何超蕸终身未婚”算同一事件
  - 伊朗相关当前分散在 `1 / 2 / 17` 的条目，按现口径应合并成一个大簇

## 未完成

- [ ] 修正 pair 与 gold 的冲突样本
  - 当前至少还有 `10` 条 `pair=1` 被 gold 拆开
  - 当前至少还有 `1` 条 `pair=0` 被 gold 合并
  - 在这些冲突处理完之前，正式闭集指标还不能直接作为最终实验结果引用

- [ ] 运行正式闭集评测
  - 在清理后的 gold 文件上重新运行 `persisted / jaccard / semantic`
  - 产出正式版 `ARI / NMI / V-measure`
  - 同时保留 pair 级 `Precision / Recall / Accuracy / F1`

- [ ] 形成论文/答辩可直接使用的结果表
  - baseline vs semantic 的对照结果
  - pair 指标 + cluster 指标
  - 简短结论与实验解释

- [ ] 实现“可用的消融入口”
  - 当前 todo 里这项仍未完成
  - 需要先确定哪些 ablation 在当前实现下真的有效

- [ ] 做有科研意义的消融实验
  - 目前不建议直接做“去掉时间衰减 / 去掉平台多样性”
  - 原因：这两个量现在还不直接控制 merge graph，直接消融会得到不干净的实验结论
  - 更合理的方向是：固定阈值替代自适应阈值、去掉簇内复核、去掉 importance 某个分量

- [ ] 如果坚持做时间/平台消融，需要先修改 `app/services/semantic_index.py`
  - 让时间衰减和平台多样性真正参与 merge 判定，而不只是影响诊断或辅助分数

## 当前阻塞

- gold 与 pair 的标注口径尚未完全统一
- 正式 `ARI / NMI / V-measure` 还不能在当前版本上直接定稿
- 消融设计与当前真实 merge 逻辑还没有完全对齐

## 建议的下一步顺序

1. 先修正冲突样本，统一 pair 与 gold 的口径
2. 再跑正式评测，冻结一版最终结果
3. 最后再决定是否补做有效消融，或者先整理论文/答辩结果表

## 备注

- 这份文档只整理现状，不包含新的代码修改计划
- 当前最重要的不是继续补脚本，而是先把标注口径清理干净