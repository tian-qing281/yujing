# 舆镜（yujing）升级路线图

> 最后更新：2026-05-12
>
> **本文定位**：记录未来升级计划与历史升级里程碑，不再包含评测冻结数据、关键决策等静态事实（已分别迁至下列专题文档）。
>
> **资料迁移说明**
> - 评测数据（W3 论文主表 / W4-W5 Agent 冻结指标） → [docs/评测指标文档.md](docs/评测指标文档.md)
> - 关键决策记录（聚类路径 / 情感双轨 / PDF 字体 / 云端部署策略等） → [docs/项目说明.md](docs/项目说明.md) §8
> - 架构与能力总览 → [docs/项目说明.md](docs/项目说明.md)
>
> **书写约定**：每项未来升级项给出「现状 / 改造点 / 验收标准 / 工作量 / 依赖 / 风险」。工作量记号：XS≤2h，S≤1d，M≤3d，L≤1w，XL≥1w。

---

# 一、未来升级规划

## 一、功能升级

### F1. 事件时间轴可视化（高 ROI）
- **现状**：事件详情仅展示标题/摘要/相关文章列表，时间维度信息被埋没。
- **改造点**：在 `EventModal` 内嵌入 ECharts 横向时间轴；节点按 `fetch_time` 与各源 `extra_info.time` 排序；hover 弹出文章浮动卡片，点击直达原文。
- **验收**：单事件 ≥3 篇相关文章时显示时间轴；轴上节点点击可跳转源页面；移动端折叠为竖向时间线。
- **工作量**：S（约 0.5 天）
- **依赖**：现有 `EventModal.vue`、`marshal_event.articles` 字段
- **风险**：低

### F2. 智能预警订阅（推送）
- **现状**：`/api/ai/alerts` 仅前端轮询展示，刷新页面即丢失，无外部触达能力。
- **改造点**：
  1. 新增 `Subscription` 表（`id, owner, keyword, source_id, threshold_heat, channel, target, enabled`）
  2. `_refresh_events_cache` 末尾比对历史事件快照，命中订阅时入队 `outgoing_alerts`
  3. 后台 worker 将告警通过 SMTP / 飞书 Webhook / 企业微信 Webhook 推送
- **验收**：用户配置「新疆 + 微博 + 热度>50万」，命中后 30 秒内收到 Webhook 通知；可在 UI 中查看推送历史。
- **工作量**：M（约 2 天）
- **依赖**：SMTP 凭据 / 飞书机器人 URL；可复用现有 `event_hub_refresh_lock`
- **风险**：中（需要轻量的去重 / 退避策略避免风暴）

### F3. Agent 多轮记忆 + 会话历史 ✅ 已交付（2026-05-15 复盘确认）
- **现状**：[AIConsultant.vue](yujing-ui/src/components/AIConsultant.vue) `sessions[]` 多会话管理 + `localStorage` 持久化 + 自动取首条 query 命名 + 历史导出 JSON
- **后端注入**：[routes.py](app/api/routes.py) `/api/mcp/ask` 取 `req.history[-4:]` 拼到 system prompt；[llm.py](app/llm.py) `chat_with_news_stream` 完整对话注入 `SystemMessage + HumanMessage/AIMessage`
- **未补**：跨设备同步（需后端持久化 + 用户体系），单机使用不影响
- **答辩话术**：`已实现多会话 + 历史持久化 + 4 轮上下文注入；跨设备需求列在 V3 路线图`

### F4. 报表导出 Word/PPT
- **现状**：仅支持 PDF 导出。
- **改造点**：用 `python-docx` + `python-pptx` 复刻 PDF 模板（封面 / 目录 / 事件分页 / 附录）；前端 ExportButton 加入格式下拉。
- **验收**：导出的 Word 可直接二次编辑；PPT 每事件一页，自带情绪卡片与词云图片。
- **工作量**：M（约 2 天）
- **风险**：低

### F5. 跨平台比较仪表盘扩展
- **现状**：`CompareDashboard` 仅支持双平台对比。
- **改造点**：升级为 N 平台雷达图（5 维：情绪倾向 / 平均热度 / 事件时延 / 主题多样性 / 跨平台重合度）。
- **工作量**：S（约 1 天）
- **风险**：低

---

## 二、性能升级

### P1. 增量聚类（高 ROI） ✅ v1.5 已落地
- **现状（已完成）**：`rebuild_events` 每次全量重建 9000 篇文章约 20 秒；本轮新增 `incremental_cluster` 增量入口，仅处理 `clustered_at IS NULL` 的最近文章，复用既有 BGE 向量与 FAISS 链路。
- **已实施**：
  1. `Article` 表新增 `clustered_at`；`Event` 表新增 `centroid` (BLOB) + `centroid_count`，启动期幂等迁移 `ensure_migrations()`
  2. `app/services/incremental_cluster.py`：余弦命中 ≥ 0.69 即挂到既有事件 + 在线均值更新 centroid；未命中走 buffer 内 mini Union-Find 形成新事件
  3. `POST /api/admin/events/incremental?lookback_hours=168&max_pending=500` 入口；scheduler 仍保留全量 `rebuild_events` 做兜底
- **实测**：首次 200 条冷启动 → 183 个新事件；第二批 200 条 → 42 条命中既有 centroid 挂载（成功率 21%），151 条新事件
- **遗留**：P1.1 全量定期校准（每 6h 一次） + 把 scheduler 切到 incremental 主路径

### P2. FAISS IVF → HNSW
- **现状**：`IVF-Flat nlist=15` 对 252 文档过拟合，FAISS 自身警告「训练点不足」。
- **改造点**：切换 HNSW32（无需训练，向量动态插入）；保留 IVF 作为大数据量后端可切换。
- **验收**：召回率 ≥ IVF 的 95%；QPS ≥ IVF 的 1.5 倍
- **工作量**：S（约 0.5 天）
- **风险**：低（FAISS 接口一致）

### P3. SSE / Agent 工具调用并行化
- **现状**：Agent loop 串行调用工具，10 工具长链最长 26 秒。
- **改造点**：检测同一批 `tool_calls` 之间是否有依赖（参数引用上一步结果即视为有依赖），无依赖则 `asyncio.gather` 并发执行；trace 时间线照旧。
- **验收**：典型「双工具调用」场景从 6s → 3s；trace 顺序仍可读
- **工作量**：S（约 1 天）
- **风险**：低

### P4. 嵌入批量化 + 模型 INT8 量化
- **现状**：BGE-small-zh 单条 forward。
- **改造点**：批量 32；启用 `torch.quantization.quantize_dynamic`；CPU 推理时启用 oneDNN。
- **验收**：批量 100 文档编码耗时 <1.5s；与 FP32 余弦相似度差异 <1%
- **工作量**：M（约 2 天）
- **风险**：中

### P5. SQLite WAL checkpoint 调度
- **现状**：长跑后 `*.wal` 文件可达 100MB+，启动慢。
- **改造点**：每 30 分钟后台执行 `PRAGMA wal_checkpoint(TRUNCATE)`。
- **工作量**：XS（约 1 小时）
- **风险**：低

---

## 三、UI 升级

### U1. 暗色主题（高 ROI）
- **现状**：仅 light，DaisyUI 自带 dark 主题但未启用。
- **改造点**：`AppHeader` 新增切换按钮，`localStorage` 持久；ECharts 双套配色（dark 用 `dark` registerTheme）。
- **工作量**：S（约 1 天）
- **风险**：低

### U2. 全局命令面板（Cmd+K）
- **现状**：跨平台搜索需进入「全景事件聚合」页。
- **改造点**：自研 Modal 或引入 `vue-command-palette`；索引内容包括所有平台 / 最近事件 / Agent 预设 / 设置项。
- **工作量**：M（约 1.5 天）
- **风险**：低

### U3. 移动端 PWA
- **现状**：移动端可看，无法安装、无离线。
- **改造点**：`vite-plugin-pwa` + manifest + Service Worker 缓存事件列表。
- **验收**：手机「添加到桌面」后离线可看缓存事件
- **工作量**：M
- **风险**：低

### U4. 骨架屏统一
- **现状**：列表/详情加载时卡片闪烁。
- **改造点**：全局 `<SkeletonCard>` 组件替换所有 spinner。
- **工作量**：S
- **风险**：低

### U5. 工作区视图保存
- **现状**：当前筛选条件刷新即丢失。
- **改造点**：保存命名预设（时间窗 + 平台 + 排序），可一键加载。
- **工作量**：S
- **风险**：低

---

## 四、自由扩展（可观测性 / 工程化）

### X1. Prometheus + Grafana
- **现状**：`/api/sync/status` 已有原始数据，缺统一指标暴露。
- **改造点**：`prometheus_client` 暴露 `/metrics`，指标含抓取耗时、事件数、Agent 工具命中率、LLM token 用量、SSE 连接数。
- **工作量**：S（半天）
- **ROI**：长期监控价值高

### X2. Sentry 错误上报
- **改造点**：后端 `sentry-sdk[fastapi]`，前端 `@sentry/vue`，免费版每月 5k 事件足够。
- **工作量**：XS（2 小时）

### X3. A/B 测试框架
- **当前判断**：答辩项目阶段可暂缓，留作后续商用化储备。

---

## 优先级建议（一周冲刺）

| 顺序 | 项目 | 收益 | 风险 |
|------|------|------|------|
| 1 | P1 增量聚类 | 立即解决全量重建瓶颈 | 中（已有 fallback） |
| 2 | F1 事件时间轴 | 答辩 Demo 加分 | 低 |
| 3 | ~~U1 暗色主题~~ | （已评估后放弃，全组件适配成本高） | — |
| 4 | P3 工具并行化 | 已在 W5 落地，延迟 37s→26s | — |
| 5 | X1 Prometheus | 演示「工程化」 | 低 |

> 本路线图属意向清单，实施前每项再单独写《变更说明》并备份原文件到 `_backups/<feature>_<timestamp>/`。

---

# 二、历史升级里程碑（W1-W7）

> 下列项均为已落地交付结果，仅保留本路线图追溯所需的「包名 · 交付物 · 状态」三要素；评测冻结数据另见 [评测指标文档](docs/评测指标文档.md)。

## A.1 总目标

两条主线并行推进：
- **答辩线**：2-3 个能在 5 分钟讲清楚的差异化亮点，live demo 稳定不翻车
- **论文线**：至少 1 套完整 baseline vs upgraded 对照实验 + 可量化指标表

## A.2 W1-W7 里程碑总表

| 周次 | 主题 | 状态 | 关键产出 |
|:---:|:---|:---:|:---|
| W1 | 语义向量化基础设施 | ✅ | BGE-small-zh 接入、`ArticleEmbedding` 表、批量物化 |
| W2 | FAISS 聚类 + 事件落库 | ✅ | `cluster_articles_semantic_faiss`、双层 Otsu、canonical 硬边 |
| W3 | 论文评测 + 消融 + Bug 修 | ✅ 冻结 | 主表 + 消融表 + 标注仲裁闭环 + canonical rescue 修复 |
| W4 | LLM Tool-Calling Agent | ✅ 冻结 | Agent 引擎 + 9 工具 + 可视化面板 + 20 query 评测（完成率 95%） |
| W5 | UI / 性能 P0 + Bug 修复 | ✅ | Agent 并发流式（37s→26s）+ 首屏拆分 + BERT batch + 5 轮前端 Bug 修复 |
| W6 | Demo 视频 + slide + 论文初稿 | ⏳ 待启 | 3 分钟 Demo、答辩 10 页、论文 8 页 |
| W7 | 订阅 / ABSA / B 站舆情 | ✅ | A1 权重可调 · A2 已读持久化 · B5 ABSA 雷达 · B6 B 站评论·弹幕情绪 · E15 主区响应式 |

> 详细评测数据（W3 主表 + 消融、W4 Agent 20 query、W5 优化后 40 query）请查 [docs/评测指标文档.md](docs/评测指标文档.md)。

## A.5 W5 已落地清单

- [x] [A] charts-vendor 首屏拆分：1093KB → 543KB（−50%）
- [x] [B] Agent 并发化（prompt 鼓励 + `asyncio.gather`）：37s → 26s
- [x] [C] BERT batch=8：`emotion.analyze_batch()` + `_run_emotion_backfill` 批量化
- [x] [E] MeiliSearch `typoTolerance` 400 修复 + 语义索引启动自动 build
- [x] [E] 流式研判打字机失效（`final_delta` + 30ms 分帧）
- [x] [E] Agent 预设条覆盖全屏修复 · OMP 重复库崩溃修复
- [x] 5 轮前端 Bug 修复（用户消息不显示 / LLM 流式 / 圆圈旋转 / 热榜空白 / 统一 Agent 模式）
- [x] 双引擎情感（`EmotionEngine` 8 维 + `SentimentEngine` c3-v2 三分类）
- [x] PDF 字体修复（`msyh.ttc`） + 副标题恢复 + 标题统一
- [x] 引用 Badge（TopicModal 文字+图标 / AIConsultant 仅图标）
- [x] 前端 API 配置化（26 处硬编码 → `buildApiUrl()`） + `VITE_API_BASE_URL`
- ❌ [D] 全局暗色主题 toggle —— 评估后回退（全组件适配差、维护成本高）

## A.6 W7 已落地清单（2026-05-09 · 本轮）

- [x] **A1 订阅打分权重可调**：`/api/recommendations` 4 维 query 权重 clamp[0,10]，前端滑杆 debounce
- [x] **A2 已读 / 已忽略持久化**：`localStorage` (`yujing.sub.readIds` / `dismissedIds`) 跨刷新
- [x] **B5 ABSA 维度雷达**：LLM 抽取 5-8 个核心方面（人物 / 机构 / 议题），雷达图渲染
- [x] **B6 B 站评论·弹幕情绪**：buvid3 cookie 预热修复 reply -352 风控 · cursor 翻页 · 本地 jsonl/xml 与实时接口去重合并 · 8 维 BERT 聚合 · 双圆环图 + 底部 legend
- [x] **E15 主区响应式**：`grid minmax(320px, 1fr) + max-width 1800px`，超宽屏不留大块白
- [x] data 样本入库：50 BV 评论·弹幕（4.7MB / 100 文件）作为本地兜底演示数据

## A.7 W6 收官（待启）

- 3 分钟 Demo 视频（5 场景）
- 答辩 slide 10 页
- 论文初稿 8 页（Abstract / Related Work / Method / Experiment / Discussion）

---

## A.8 后续规划（不阻塞答辩）

### S1. 订阅关键词召回从字符串子串升级为语义召回 ✅ v1.5 已落地

- **落地实现**（v1.5）：
  - 新建 [app/services/subscription_semantic.py](app/services/subscription_semantic.py)：
    `score_events_for_subscriptions(events, sub_values, threshold=0.40)`，复用 P1
    写入的 `Event.centroid`（BGE-small-zh L2 归一向量），订阅词通过 `_get_sub_vector`
    现编码 + 进程内缓存（cache key 含 `EMBED_MODEL_NAME`，换模型不命中错向量）。
  - 改造 `/api/recommendations`：新增 query 参数 `use_semantic=true`（默认开）/
    `w_semantic=6.0` / `semantic_threshold=0.40`；语义命中分 = `cosine × w_semantic × weight`，
    与字面 `kw in title` 同位合并到 `sub_weight_map`，去重保护：仅当订阅词不出现在
    标题中时才叠加语义分（避免双重加权）。
  - 响应字段新增：`semantic_used / semantic_hits / weights.semantic / semantic_threshold`；
    `_recommend_reasons` 显示「语义相似「人工智能」 0.42」。
  - 实测命中：订阅「房地产」召回「未来房子只有两种」(0.42)、订阅「美国」召回
    「中美关系发展方向」(0.48)、「中美元首北京会晤」(0.43)，均为字面无法召回的事件。
- **后续可优化**：阈值灰度、订阅 CRUD 时持久化向量到 `subscriptions.embedding_blob`、
  前端命中标签 UI 展示语义分；本期 MVP 暂不动 schema、不动前端。

### S1.2 订阅词向量持久化 ✅ v1.6 已落地

- DB：`subscriptions.embedding`（512 dim float32 BLOB，L2 归一），`ensure_migrations` 幂等 ALTER
- 创建订阅时调 `encode_and_persist_subscription(db, sub)` 立即编码落库
- 推荐入口 `prime_cache_from_db(db, subs)` 把 DB 向量灌进进程缓存，重启后冷启动也瞬时命中
- 仅 `kind ∈ {keyword, event}` 编码，`source` 类不需要

---

## A.9 W8 已落地清单（2026-05-15 · 本轮编辑式 UI 重构 + 影响指数差异化）

> 主题：editorial 杂志化排版 + 微动效收敛 + 数据视觉一致性，所有改动按 Batch 切片提交，每批均做浏览器走查验证。

### A · UI / 排版（已交付 6 批）

- [x] **Batch E** · NewsCard / EventCard editorial 化：序号通栏字号 + serif 标题 + 副条紧凑层级；移除糖果蓝阴影
- [x] **Batch F** · 杂志网格 magazine-grid：通栏 lead + 4 副条 major + 第 6+ row variant；列表过渡平滑
- [x] **Batch F.1 + V** · 头条卡 `--lead` 缩小：padding 32→22 / gap 18→14 / rank 44→34 / title 26→22，与 02-05 副条视觉差距收窄
- [x] **Batch H + H.1** · 全局糖果蓝清扫：14+ 处残留 hover/border/icon 改 token 化（neutral-warm + accent-coral），微动效统一 200ms ease-out（保留情感色：负面 #dc2626 / 中性 #94a3b8 / 正面 #16a34a）
- [x] **Batch I** · NumberFlow 数字滚动 14 处位点（事件 / 热搜 / 三数对照 / Agent 会话 / 订阅命中数 / 匹配分），自带 tabular-nums + 千位分隔
- [x] **Batch II** · ECharts 入场动画统一：`utils/chartAnimation.js` 注入 ANIM.{bar,pie,radar,line}，11 处 chart 接入
- [x] **Batch III** · 全局排版 baseline：`body` 行高 1.7 + h1-h6 letter-spacing -0.01em + `meta/-count/-stat` tabular-nums + 主要按钮字距收紧
- [x] **Batch IV** · ABSA 维度雷达修复：极性映射 1/2/3 杜绝塌中心 + 顶点按情感染色（红/灰/绿）

### B · 数据视觉一致性（已交付）

- [x] **F1** · 影响指数 per-source 差异化：`recomputeImpactByGroup` 按 source_id 分组用榜单内排名重算；baseScore 上限 92 留 8 分顶部；平台 bonus 除数差异化（微博 hot_score/500K · 知乎 view_count/3M · B站 view/1.5M · 百度 hot_score/5M · 头条 hot_value/800M）；不再被后端 `rank=99` 占位污染。验证：百度 93/91/90/88/86 · 知乎 99/97/96/94/93 梯度清晰。

### 待办优先级（接下来冲刺）

> 按 ROI 从高到低，每完成一项立即浏览器走查 + 单 commit。

#### 第一梯队 · UI 完成度收口（已交付）

- [x] **Batch VI** · 暗色磁吸侧栏 active 视觉强化：背景 rgba(255,255,255,0.12) + 字重 600 + inset 3px 橙描边 + inset 1px 描边，与 hover 0.08 拉开层级（commit `189a555`）
- [x] **Batch VII** · 卡片 hover 微动效统一：NewsCard / EventCard / TopicCard 全部 200ms ease-out（transform/box-shadow/border-color 三属性一致）（commit `00861c4`）
- [x] **Batch VIII** · MoreHeadlines row 右侧加影响指数小数字：13px / 字重 500 / muted 色 / tabular-nums，与序号、标题同 baseline；微博榜 06=84 → 09=79 与 lead/major 91/85 梯度无缝（commit `00861c4`）
- [x] ~~**Batch IX**~~ · 凭据资产配置弹窗 editorial 化：实际已在 Batch H/H.1 完成（按钮已 token 化，糖果蓝→ var(--color-brand)，无残留）
- [x] ~~**Batch X**~~ · AnalysisModal / EventModal tab 视觉收敛：实际两个 Modal 用胶囊态切换（cap-tab / eh-sort-btn）非下划线 tab，已是 active 块切换形态，无优化空间

#### 第二梯队 · 功能升级（融合旧 F2-F5）

- [ ] **F2** · 智能预警订阅推送（旧路线图 F2，未启）：Webhook + SMTP + 飞书机器人；命中后 30s 内触达
- [ ] **F3** · Agent 多轮记忆（旧 F3，未启）：`conversation_id` + `Conversation/Message` 表 + 最近 4 轮摘要注入
- [ ] **F4** · 报表导出 Word/PPT（旧 F4，未启）：`python-docx` + `python-pptx` 复刻 PDF 模板
- [ ] **F5** · 跨平台比较 N 平台雷达（旧 F5，未启）：5 维（情绪 / 热度 / 时延 / 多样性 / 重合度）
- [ ] **F6** · ABSA 维度雷达升级版：维度横向时间轴（同一 aspect 在不同时间点的情感漂移）
- [x] **F7** · 影响指数算法可解释面板：`dataAdapter.explainImpact()` 给出 4 行人话拆解（来源·排名 / 基础分 / 平台热度加权 / 合计）；NewsCard heat badge + row 数字 hover 显示。验证 tooltip：`微博 · 第 1 名 / 基础分 91 / 热度值 0 → +0.0 / 合计 91/99`

#### 第三梯队 · 工程化收口

- [x] ~~**J**~~ · 后端 rank=99 占位：实际已在 J/K 合并修复中走 `setdefault("rank", idx)` 兜底，无需改 nullable 策略
- [x] **K** · 全平台 listIndex 兜底迁移：`base.py:_save_to_db` 入口 enumerate 注入 rank，覆盖 baidu/bilibili/toutiao/zhihu 4 个未塞 rank 的 source；前端 `recomputeImpactByGroup` 保留作为防御层 + 搜索页相关度近似
- [ ] **L** · X1 Prometheus（旧 X1）：`/metrics` 暴露抓取耗时 / 事件数 / Agent 工具命中率 / LLM token 用量

### 推进顺序建议

1. ~~先把第一梯队 Batch VI-X 全部交付~~ ✅ 已清完
2. ~~再做 F7（可解释面板，强答辩点）~~ ✅ 已交付
3. ~~J/K 后端 rank 兜底~~ ✅ 已交付
4. 接下来 F2 推送（半天）+ F3 Agent 记忆（1.5 天）
5. F4/F5/F6 视答辩窗口期取舍

> 记忆要点：每改一步浏览器走查 + 单文件 commit message 用 `feat(batch-X): xxx` / `feat(f-N): xxx` 前缀，方便后续回溯。

---

## 最近交付节点（追加式日志）

> 本节统一收口"日度总结 / 验证报告"类临时文档。以后不再新建独立 `YYYY-MM-DD-总结.md`，所有交付节点追加在这里。

### 2026-05-15 · W8 收口 + J/K + F7

**Commit 链**：`d70e2ca` → `1e4dd89` → `189a555` → `00861c4` → `b4784b2` → `df7d3dd` → `7f55c01`

**交付项**：
- F1 漏洞 1+3+T2 三处加固（搜索语义 JSDoc / `Math.min(99, ...)` 硬上限 / 时序依赖修复）
- Batch VI 侧栏 active 视觉强化
- Batch VII/VIII 卡片 hover 200ms ease-out + MoreHeadlines row 影响指数
- **J/K** 后端 `setdefault("rank", idx)` 兜底（[app/crawler/sources/base.py](app/crawler/sources/base.py)）：彻底修复 baidu/bilibili/toutiao/zhihu 4 源 `rank=99` 占位
- **F7** 影响指数可解释面板（`dataAdapter.explainImpact()` + NewsCard tooltip）
- 文档：[对比分析-鱼皮热点监控.md](docs/对比分析-鱼皮热点监控.md)

**真实生效验证**（DB 查询，rank=999 占位排除）：

| source_id | rank 范围 | rank=99 占比 |
|---|---|---|
| baidu_hot / bilibili_hot_video / toutiao_hot / zhihu_hot_question | 1-30 | **0** ✅ |
| weibo_hot_search | 1-30 | 0 |
| thepaper_hot / wallstreetcn_news / cls_telegraph | 1-N | 0 |

**8 榜 F7 tooltip 抽样**：B 站第 1 名「基础分 91 / 播放量 6.46M → +4.3 / 合计 95」；澎湃/华尔街/财联社显示「该平台仅按排名计分，无热度加权」优雅 fallback；rank 1-5 梯度严格匹配 `max(25, round(92-rank*1.4))` = 91/89/88/86/85。

**API 验证**：`GET /api/sync/status` 200；`GET /api/ai/morning_brief/pdf` 200 / 208KB / 2.44s。

**已知约束**（非 bug，留作后续优化）：
1. T1 工科榜分数偏低：头条/百度/微博 `hot_value` 缩放系数 `0.0000002` 偏小，让排名主导；可在 `BONUS_FIELD_MAP` 单独调整
2. 微博 / 知乎 `hot_value=0`：源数据缺字段，tooltip 显示 `→ +0.0` 是正常 fallback
3. B 站 cookie 失效偶发，需手工凭据弹窗刷新


### V2 画像兴趣 tag 升级（embedding 邻近召回） ✅ v1.6 已落地

- **痛点**：旧 `top_tags` 来自 `article.extra_info.category`，>90% 文章只有「综合」，画像 tag 维度形同虚设
- **新做法**：[app/services/profile_inference.py](app/services/profile_inference.py)
  - 取最近 50 条 view_history（action 加权：view=1 / open=2 / dwell=clamp(ms/5000, 1, 5)）
  - 加权聚合 BGE article embedding → L2 归一为「用户兴趣向量」centroid
  - 用 centroid 邻近召回最近 7 天 cos≥0.55 的 Top80 篇文章，join `Event.keywords` 抽取真实关键词
  - 累计权重排序得 Top12 inferred_tags，30 min TTL
- **实测**（78 view_history → 26 篇 → 1690 候选 → 80 命中）：
  特朗普(5.95) / 美国(5.32) / ai(4.69) / 中国(4.61) / 母亲节(3.99) / 总统(3.26)
- **冷启动**：`MIN_HISTORY_FOR_INFER=5`，浏览不足 5 篇时返回空数组

### A1 推荐解释徽章结构化 ✅ v1.6 已落地

- 后端：`scored` 元组 3→4 元 `(score, ev, reasons, chips)`，响应增加 `_recommend_chips`
- chip 类型 6 类：keyword / sub_source / profile_source / profile_tag / semantic / fallback
- 前端：6 色徽章渲染 + hover tooltip 完整公式（如 `订阅词「美国」字面命中标题 +5`）
- 同时删除冗余「兴趣标签 TOP」block（旧 `top_tags` 只能显示「综合」）

---

### S1（原始规划备份）

- **现状**：`_today_recommendations`（routes.py L3683）的关键词命中是 `kw in title`
  字面子串匹配 —— 订阅"半导体"召不到"芯片产业链"标题，订阅"AI"召不到"人工智能"。
  完整算法见 7 维加权排序：① 订阅 keyword/event 命中 +5×weight ② 订阅 source 命中
  事件主源 +3×weight ③ 画像 source/tag 偏好按比例 0~+3 ④ blocklist 黑词剔除
  ⑤ 同分按事件 heat_score 兜底 ⑥ fallback 模式打 _fallback=true 防空白页 ⑦ RAG
  has_keyword_match 召回门槛同样字面匹配。
- **痛点**：用户需手动列举所有同义词，体感像"古早搜索引擎"，与"AI 智能体舆情系统"
  的产品定位不符。
- **方案**（二选一或叠加）：
  1. **embedding 语义召回**：复用现有 `bge-small-zh-v1.5`（W1 已接入），订阅词
     与候选事件标题/摘要分别向量化，余弦相似度 ≥ 阈值（建议 0.55）即视为命中，
     得分 = `cosine × 5 × weight`。订阅词向量可缓存到 `subscriptions.embedding_blob`
     字段，事件向量已在 `EventEmbedding`（如无则补建）。
  2. **LLM 同义词扩展**：用户配置订阅词时，后端调一次 DeepSeek 让其产出 3-5 个
     同义词/上位词（如"半导体" → ["芯片", "晶圆", "IC", "集成电路"]），存到
     `subscriptions.synonyms` JSON 字段，命中检测时遍历同义词。优点：零延迟
     在线推理；缺点：依赖一次 LLM 调用，需缓存 + 失败兜底。
- **验收标准**：
  - 订阅"半导体"，今日推荐应能召回标题含"芯片/晶圆/IC"的事件
  - `_today_recommendations` 响应增加 `match_type: 'literal' | 'semantic' | 'synonym'`
    字段，前端订阅命中标签可显示"语义命中 0.72"
  - 关闭语义开关时回退到字面匹配（保持向后兼容）
- **工作量**：M（约 2 天，含订阅 CRUD 改造 + 前端命中标签 UI）
- **依赖**：`bge-small-zh-v1.5`（已接入）/ `EventEmbedding` 表 / DeepSeek API
- **风险**：低；语义阈值需小流量灰度调参，过低可能引入噪声推荐
- 一键启动脚本 `scripts/demo.ps1`
- 回归测试：W3 评测集 P/R/F1 不回退，生产 rebuild 5000+ 事件无崩溃

> 关键设计决策（聚类路径 / 情感双轨 / BERT 后台精标 / PDF 字体 / API 配置化 / 云端部署）请查 [docs/项目说明.md](docs/项目说明.md) §8。

## A.9 被砍 / 延后项目

- 公网部署 / 鉴权限流 / HTTPS
- PostgreSQL 迁移
- 移动端 PWA（U3 仍在路线图，未启动）
- 立场分析
- 定时日报邮件推送
- 暗色主题（U1 / D，回退后明确放弃）
