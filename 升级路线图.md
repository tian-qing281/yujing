# 舆镜（yujing）升级路线图

> 最后更新：2026-05-19（UI-15/16 全部弹层 + C 深层数据组件编辑部化完成 + UI-17 composer 对比度修复 + UI-18 CompareDashboard 窄气泡塌列修复 + 全站同步 0/8 卡死排查日志落盘 + per-crawler 30s 超时保护 + 前端同步失败 toast + **/sync/status 双 bug 终极修复**）
>
> **本文定位**：记录未来升级计划与历史升级里程碑，不再包含评测冻结数据、关键决策等静态事实（已分别迁至下列专题文档）。
>
> **资料迁移说明**
> - 评测数据（W3 论文主表 / W4-W5 Agent 冻结指标） → [docs/评测指标文档.md](docs/评测指标文档.md)
> - 关键决策记录（聚类路径 / 情感双轨 / PDF 字体 / 云端部署策略等） → [docs/项目说明.md](docs/项目说明.md) §8
> - 架构与能力总览 → [docs/项目说明.md](docs/项目说明.md)
>
> **书写约定**：每项未来升级项给出「现状 / 改造点 / 验收标准 / 工作量 / 依赖 / 风险」。工作量记号：XS≤2h，S≤1d，M≤3d，L≤1w，XL≥1w。

---

# 一、未来升级规划

## 一、功能升级

### F1. 事件时间轴可视化（高 ROI）
- **现状**：事件详情仅展示标题/摘要/相关文章列表，时间维度信息被埋没。
- **改造点**：在 `EventModal` 内嵌入 ECharts 横向时间轴；节点按 `fetch_time` 与各源 `extra_info.time` 排序；hover 弹出文章浮动卡片，点击直达原文。
- **验收**：单事件 ≥3 篇相关文章时显示时间轴；轴上节点点击可跳转源页面；移动端折叠为竖向时间线。
- **工作量**：S（约 0.5 天）
- **依赖**：现有 `EventModal.vue`、`marshal_event.articles` 字段
- **风险**：低

### F2. 智能预警订阅（推送）
- **现状**：`/api/ai/alerts` 仅前端轮询展示，刷新页面即丢失，无外部触达能力。
- **改造点**：
  1. 新增 `Subscription` 表（`id, owner, keyword, source_id, threshold_heat, channel, target, enabled`）
  2. `_refresh_events_cache` 末尾比对历史事件快照，命中订阅时入队 `outgoing_alerts`
  3. 后台 worker 将告警通过 SMTP / 飞书 Webhook / 企业微信 Webhook 推送
- **验收**：用户配置「新疆 + 微博 + 热度>50万」，命中后 30 秒内收到 Webhook 通知；可在 UI 中查看推送历史。
- **工作量**：M（约 2 天）
- **依赖**：SMTP 凭据 / 飞书机器人 URL；可复用现有 `event_hub_refresh_lock`
- **风险**：中（需要轻量的去重 / 退避策略避免风暴）

### F3. Agent 多轮记忆 + 会话历史 ✅ 已交付（2026-05-15 复盘确认）
- **现状**：[AIConsultant.vue](yujing-ui/src/components/AIConsultant.vue) `sessions[]` 多会话管理 + `localStorage` 持久化 + 自动取首条 query 命名 + 历史导出 JSON
- **后端注入**：[routes.py](app/api/routes.py) `/api/mcp/ask` 取 `req.history[-4:]` 拼到 system prompt；[llm.py](app/llm.py) `chat_with_news_stream` 完整对话注入 `SystemMessage + HumanMessage/AIMessage`
- **未补**：跨设备同步（需后端持久化 + 用户体系），单机使用不影响
- **答辩话术**：`已实现多会话 + 历史持久化 + 4 轮上下文注入；跨设备需求列在 V3 路线图`

### F4. 报表导出 Word/PPT ✅ 已交付（2026-05-16）
- **已实现**：`app/services/report_export.py` 新增 `build_docx` / `build_pptx`，与现有 PDF 模板**同源同结构**（封面页 + 章节标题层级 + 行内 **粗体** + 截图嵌入）
- **接入点**：`/api/articles/{id}/export_pdf`、`/api/events/{id}/export_pdf`、`/api/ai/morning_brief/pdf`、`/api/ai/export_pdf` 四端点统一加 `?format=pdf|docx|pptx`
- **前端**：AIConsultant 早报 banner + 消息卡片、BriefModal 详情页均升级为「PDF / Word / PPT」三按钮组
- **依赖**：python-docx、python-pptx 1.0.2、Pillow
- **演示价值**：评委可现场点击三个按钮直接下载 .pdf / .docx / .pptx，Word 可二次编辑、PPT 直接答辩复用

### F5. 跨平台比较仪表盘扩展
- **现状**：`CompareDashboard` 仅支持双平台对比。
- **改造点**：升级为 N 平台雷达图（5 维：情绪倾向 / 平均热度 / 事件时延 / 主题多样性 / 跨平台重合度）。
- **工作量**：S（约 1 天）
- **风险**：低

---

## 二、性能升级

### P1. 增量聚类（高 ROI） ✅ v1.5 已落地
- **现状（已完成）**：`rebuild_events` 每次全量重建 9000 篇文章约 20 秒；本轮新增 `incremental_cluster` 增量入口，仅处理 `clustered_at IS NULL` 的最近文章，复用既有 BGE 向量与 FAISS 链路。
- **已实施**：
  1. `Article` 表新增 `clustered_at`；`Event` 表新增 `centroid` (BLOB) + `centroid_count`，启动期幂等迁移 `ensure_migrations()`
  2. `app/services/incremental_cluster.py`：余弦命中 ≥ 0.69 即挂到既有事件 + 在线均值更新 centroid；未命中走 buffer 内 mini Union-Find 形成新事件
  3. `POST /api/admin/events/incremental?lookback_hours=168&max_pending=500` 入口；scheduler 仍保留全量 `rebuild_events` 做兜底
- **实测**：首次 200 条冷启动 → 183 个新事件；第二批 200 条 → 42 条命中既有 centroid 挂载（成功率 21%），151 条新事件
- **遗留**：P1.1 全量定期校准（每 6h 一次） + 把 scheduler 切到 incremental 主路径

### P2. FAISS IVF → HNSW
- **现状**：`IVF-Flat nlist=15` 对 252 文档过拟合，FAISS 自身警告「训练点不足」。
- **改造点**：切换 HNSW32（无需训练，向量动态插入）；保留 IVF 作为大数据量后端可切换。
- **验收**：召回率 ≥ IVF 的 95%；QPS ≥ IVF 的 1.5 倍
- **工作量**：S（约 0.5 天）
- **风险**：低（FAISS 接口一致）

### P3. SSE / Agent 工具调用并行化
- **现状**：Agent loop 串行调用工具，10 工具长链最长 26 秒。
- **改造点**：检测同一批 `tool_calls` 之间是否有依赖（参数引用上一步结果即视为有依赖），无依赖则 `asyncio.gather` 并发执行；trace 时间线照旧。
- **验收**：典型「双工具调用」场景从 6s → 3s；trace 顺序仍可读
- **工作量**：S（约 1 天）
- **风险**：低

### P4. 嵌入批量化 + 模型 INT8 量化
- **现状**：BGE-small-zh 单条 forward。
- **改造点**：批量 32；启用 `torch.quantization.quantize_dynamic`；CPU 推理时启用 oneDNN。
- **验收**：批量 100 文档编码耗时 <1.5s；与 FP32 余弦相似度差异 <1%
- **工作量**：M（约 2 天）
- **风险**：中

### P5. SQLite WAL checkpoint 调度
- **现状**：长跑后 `*.wal` 文件可达 100MB+，启动慢。
- **改造点**：每 30 分钟后台执行 `PRAGMA wal_checkpoint(TRUNCATE)`。
- **工作量**：XS（约 1 小时）
- **风险**：低

---

## 三、UI 升级

### U1. 暗色主题（高 ROI）
- **现状**：仅 light，DaisyUI 自带 dark 主题但未启用。
- **改造点**：`AppHeader` 新增切换按钮，`localStorage` 持久；ECharts 双套配色（dark 用 `dark` registerTheme）。
- **工作量**：S（约 1 天）
- **风险**：低

### U2. 全局命令面板（Cmd+K）
- **现状**：跨平台搜索需进入「全景事件聚合」页。
- **改造点**：自研 Modal 或引入 `vue-command-palette`；索引内容包括所有平台 / 最近事件 / Agent 预设 / 设置项。
- **工作量**：M（约 1.5 天）
- **风险**：低

### U3. 移动端 PWA
- **现状**：移动端可看，无法安装、无离线。
- **改造点**：`vite-plugin-pwa` + manifest + Service Worker 缓存事件列表。
- **验收**：手机「添加到桌面」后离线可看缓存事件
- **工作量**：M
- **风险**：低

### U4. 骨架屏统一
- **现状**：列表/详情加载时卡片闪烁。
- **改造点**：全局 `<SkeletonCard>` 组件替换所有 spinner。
- **工作量**：S
- **风险**：低

### U5. 工作区视图保存
- **现状**：当前筛选条件刷新即丢失。
- **改造点**：保存命名预设（时间窗 + 平台 + 排序），可一键加载。
- **工作量**：S
- **风险**：低

---

## 四、自由扩展（可观测性 / 工程化）

### X1. Prometheus + Grafana
- **现状**：`/api/sync/status` 已有原始数据，缺统一指标暴露。
- **改造点**：`prometheus_client` 暴露 `/metrics`，指标含抓取耗时、事件数、Agent 工具命中率、LLM token 用量、SSE 连接数。
- **工作量**：S（半天）
- **ROI**：长期监控价值高

### X2. Sentry 错误上报
- **改造点**：后端 `sentry-sdk[fastapi]`，前端 `@sentry/vue`，免费版每月 5k 事件足够。
- **工作量**：XS（2 小时）

### X3. A/B 测试框架
- **当前判断**：答辩项目阶段可暂缓，留作后续商用化储备。

---

## 优先级建议（一周冲刺）

| 顺序 | 项目 | 收益 | 风险 |
|------|------|------|------|
| 1 | P1 增量聚类 | 立即解决全量重建瓶颈 | 中（已有 fallback） |
| 2 | F1 事件时间轴 | 答辩 Demo 加分 | 低 |
| 3 | ~~U1 暗色主题~~ | （已评估后放弃，全组件适配成本高） | — |
| 4 | P3 工具并行化 | 已在 W5 落地，延迟 37s→26s | — |
| 5 | X1 Prometheus | 演示「工程化」 | 低 |

> 本路线图属意向清单，实施前每项再单独写《变更说明》并备份原文件到 `_backups/<feature>_<timestamp>/`。

---

# 二、历史升级里程碑（W1-W7）

> 下列项均为已落地交付结果，仅保留本路线图追溯所需的「包名 · 交付物 · 状态」三要素；评测冻结数据另见 [评测指标文档](docs/评测指标文档.md)。

## A.1 总目标

两条主线并行推进：
- **答辩线**：2-3 个能在 5 分钟讲清楚的差异化亮点，live demo 稳定不翻车
- **论文线**：至少 1 套完整 baseline vs upgraded 对照实验 + 可量化指标表

## A.2 W1-W7 里程碑总表

| 周次 | 主题 | 状态 | 关键产出 |
|:---:|:---|:---:|:---|
| W1 | 语义向量化基础设施 | ✅ | BGE-small-zh 接入、`ArticleEmbedding` 表、批量物化 |
| W2 | FAISS 聚类 + 事件落库 | ✅ | `cluster_articles_semantic_faiss`、双层 Otsu、canonical 硬边 |
| W3 | 论文评测 + 消融 + Bug 修 | ✅ 冻结 | 主表 + 消融表 + 标注仲裁闭环 + canonical rescue 修复 |
| W4 | LLM Tool-Calling Agent | ✅ 冻结 | Agent 引擎 + 9 工具 + 可视化面板 + 20 query 评测（完成率 95%） |
| W5 | UI / 性能 P0 + Bug 修复 | ✅ | Agent 并发流式（37s→26s）+ 首屏拆分 + BERT batch + 5 轮前端 Bug 修复 |
| W6 | Demo 视频 + slide + 论文初稿 | ⏳ 待启 | 3 分钟 Demo、答辩 10 页、论文 8 页 |
| W7 | 订阅 / ABSA / B 站舆情 | ✅ | A1 权重可调 · A2 已读持久化 · B5 ABSA 雷达 · B6 B 站评论·弹幕情绪 · E15 主区响应式 |

> 详细评测数据（W3 主表 + 消融、W4 Agent 20 query、W5 优化后 40 query）请查 [docs/评测指标文档.md](docs/评测指标文档.md)。

## A.5 W5 已落地清单

- [x] [A] charts-vendor 首屏拆分：1093KB → 543KB（−50%）
- [x] [B] Agent 并发化（prompt 鼓励 + `asyncio.gather`）：37s → 26s
- [x] [C] BERT batch=8：`emotion.analyze_batch()` + `_run_emotion_backfill` 批量化
- [x] [E] MeiliSearch `typoTolerance` 400 修复 + 语义索引启动自动 build
- [x] [E] 流式研判打字机失效（`final_delta` + 30ms 分帧）
- [x] [E] Agent 预设条覆盖全屏修复 · OMP 重复库崩溃修复
- [x] 5 轮前端 Bug 修复（用户消息不显示 / LLM 流式 / 圆圈旋转 / 热榜空白 / 统一 Agent 模式）
- [x] 双引擎情感（`EmotionEngine` 8 维 + `SentimentEngine` c3-v2 三分类）
- [x] PDF 字体修复（`msyh.ttc`） + 副标题恢复 + 标题统一
- [x] 引用 Badge（TopicModal 文字+图标 / AIConsultant 仅图标）
- [x] 前端 API 配置化（26 处硬编码 → `buildApiUrl()`） + `VITE_API_BASE_URL`
- ❌ [D] 全局暗色主题 toggle —— 评估后回退（全组件适配差、维护成本高）

## A.6 W7 已落地清单（2026-05-09 · 本轮）

- [x] **A1 订阅打分权重可调**：`/api/recommendations` 4 维 query 权重 clamp[0,10]，前端滑杆 debounce
- [x] **A2 已读 / 已忽略持久化**：`localStorage` (`yujing.sub.readIds` / `dismissedIds`) 跨刷新
- [x] **B5 ABSA 维度雷达**：LLM 抽取 5-8 个核心方面（人物 / 机构 / 议题），雷达图渲染
- [x] **B6 B 站评论·弹幕情绪**：buvid3 cookie 预热修复 reply -352 风控 · cursor 翻页 · 本地 jsonl/xml 与实时接口去重合并 · 8 维 BERT 聚合 · 双圆环图 + 底部 legend
- [x] **E15 主区响应式**：`grid minmax(320px, 1fr) + max-width 1800px`，超宽屏不留大块白
- [x] data 样本入库：50 BV 评论·弹幕（4.7MB / 100 文件）作为本地兜底演示数据

## A.7 W6 收官（待启）

- 3 分钟 Demo 视频（5 场景）
- 答辩 slide 10 页
- 论文初稿 8 页（Abstract / Related Work / Method / Experiment / Discussion）

---

## A.8 后续规划（不阻塞答辩）

### S1. 订阅关键词召回从字符串子串升级为语义召回 ✅ v1.5 已落地

- **落地实现**（v1.5）：
  - 新建 [app/services/subscription_semantic.py](app/services/subscription_semantic.py)：
    `score_events_for_subscriptions(events, sub_values, threshold=0.40)`，复用 P1
    写入的 `Event.centroid`（BGE-small-zh L2 归一向量），订阅词通过 `_get_sub_vector`
    现编码 + 进程内缓存（cache key 含 `EMBED_MODEL_NAME`，换模型不命中错向量）。
  - 改造 `/api/recommendations`：新增 query 参数 `use_semantic=true`（默认开）/
    `w_semantic=6.0` / `semantic_threshold=0.40`；语义命中分 = `cosine × w_semantic × weight`，
    与字面 `kw in title` 同位合并到 `sub_weight_map`，去重保护：仅当订阅词不出现在
    标题中时才叠加语义分（避免双重加权）。
  - 响应字段新增：`semantic_used / semantic_hits / weights.semantic / semantic_threshold`；
    `_recommend_reasons` 显示「语义相似「人工智能」 0.42」。
  - 实测命中：订阅「房地产」召回「未来房子只有两种」(0.42)、订阅「美国」召回
    「中美关系发展方向」(0.48)、「中美元首北京会晤」(0.43)，均为字面无法召回的事件。
- **后续可优化**：阈值灰度、订阅 CRUD 时持久化向量到 `subscriptions.embedding_blob`、
  前端命中标签 UI 展示语义分；本期 MVP 暂不动 schema、不动前端。

### S1.2 订阅词向量持久化 ✅ v1.6 已落地

- DB：`subscriptions.embedding`（512 dim float32 BLOB，L2 归一），`ensure_migrations` 幂等 ALTER
- 创建订阅时调 `encode_and_persist_subscription(db, sub)` 立即编码落库
- 推荐入口 `prime_cache_from_db(db, subs)` 把 DB 向量灌进进程缓存，重启后冷启动也瞬时命中
- 仅 `kind ∈ {keyword, event}` 编码，`source` 类不需要

---

## A.9 W8 已落地清单（2026-05-15 · 本轮编辑式 UI 重构 + 影响指数差异化）

> 主题：editorial 杂志化排版 + 微动效收敛 + 数据视觉一致性，所有改动按 Batch 切片提交，每批均做浏览器走查验证。

### A · UI / 排版（已交付 6 批）

- [x] **Batch E** · NewsCard / EventCard editorial 化：序号通栏字号 + serif 标题 + 副条紧凑层级；移除糖果蓝阴影
- [x] **Batch F** · 杂志网格 magazine-grid：通栏 lead + 4 副条 major + 第 6+ row variant；列表过渡平滑
- [x] **Batch F.1 + V** · 头条卡 `--lead` 缩小：padding 32→22 / gap 18→14 / rank 44→34 / title 26→22，与 02-05 副条视觉差距收窄
- [x] **Batch H + H.1** · 全局糖果蓝清扫：14+ 处残留 hover/border/icon 改 token 化（neutral-warm + accent-coral），微动效统一 200ms ease-out（保留情感色：负面 #dc2626 / 中性 #94a3b8 / 正面 #16a34a）
- [x] **Batch I** · NumberFlow 数字滚动 14 处位点（事件 / 热搜 / 三数对照 / Agent 会话 / 订阅命中数 / 匹配分），自带 tabular-nums + 千位分隔
- [x] **Batch II** · ECharts 入场动画统一：`utils/chartAnimation.js` 注入 ANIM.{bar,pie,radar,line}，11 处 chart 接入
- [x] **Batch III** · 全局排版 baseline：`body` 行高 1.7 + h1-h6 letter-spacing -0.01em + `meta/-count/-stat` tabular-nums + 主要按钮字距收紧
- [x] **Batch IV** · ABSA 维度雷达修复：极性映射 1/2/3 杜绝塌中心 + 顶点按情感染色（红/灰/绿）

### B · 数据视觉一致性（已交付）

- [x] **F1** · 影响指数 per-source 差异化：`recomputeImpactByGroup` 按 source_id 分组用榜单内排名重算；baseScore 上限 92 留 8 分顶部；平台 bonus 除数差异化（微博 hot_score/500K · 知乎 view_count/3M · B站 view/1.5M · 百度 hot_score/5M · 头条 hot_value/800M）；不再被后端 `rank=99` 占位污染。验证：百度 93/91/90/88/86 · 知乎 99/97/96/94/93 梯度清晰。

### 待办优先级（接下来冲刺）

> 按 ROI 从高到低，每完成一项立即浏览器走查 + 单 commit。

#### 第一梯队 · UI 完成度收口（已交付）

- [x] **Batch VI** · 暗色磁吸侧栏 active 视觉强化：背景 rgba(255,255,255,0.12) + 字重 600 + inset 3px 橙描边 + inset 1px 描边，与 hover 0.08 拉开层级（commit `189a555`）
- [x] **Batch VII** · 卡片 hover 微动效统一：NewsCard / EventCard / TopicCard 全部 200ms ease-out（transform/box-shadow/border-color 三属性一致）（commit `00861c4`）
- [x] **Batch VIII** · MoreHeadlines row 右侧加影响指数小数字：13px / 字重 500 / muted 色 / tabular-nums，与序号、标题同 baseline；微博榜 06=84 → 09=79 与 lead/major 91/85 梯度无缝（commit `00861c4`）
- [x] ~~**Batch IX**~~ · 凭据资产配置弹窗 editorial 化：实际已在 Batch H/H.1 完成（按钮已 token 化，糖果蓝→ var(--color-brand)，无残留）
- [x] ~~**Batch X**~~ · AnalysisModal / EventModal tab 视觉收敛：实际两个 Modal 用胶囊态切换（cap-tab / eh-sort-btn）非下划线 tab，已是 active 块切换形态，无优化空间

#### 第二梯队 · 功能升级（融合旧 F2-F5）

- [ ] **F2** · 智能预警订阅推送（未启）：Webhook + SMTP + 飞书机器人；命中后 30s 内触达
- [x] **F3** · Agent 多轮记忆（2026-05-15 复盘确认 ✅）：AIConsultant `sessions[]` 多会话 + `localStorage` 持久化 + `req.history[-4:]` 后端注入 system prompt（详 §F3 已交付节）
- [x] **F4** · 报表导出 Word/PPT（2026-05-16 ✅）：`build_docx` / `build_pptx` 与 PDF 同源同结构，四端点统一 `?format=pdf|docx|pptx`（详 §F4 已交付节）
- [x] **F5** · 跨平台比较 N 平台雷达（2026-05-16 ✅）：5 维（情报量 / 事件覆盖 / 情绪鲜明度 / 24h时效 / 单事件规模），后端 `compare_platforms_radar` Agent 工具 + 前端 CompareDashboard radar 分支
- [x] **F6** · ABSA 维度时间漂移（2026-05-17 ✅）：后端 `/api/events/{id}/absa_timeline` 聚合事件文章的 ABSA 文件缓存（不触发新 LLM），按 pub_date 分时间桶 → top_k aspect 极性均值；前端 `AbsaTimelineChart.vue` ECharts multi-line + 顶点情感染色（红/灰/绿）+ 点大小≈证据数；EventModal 数据分析后追加"维度时间漂移" section；预热脚本 `scripts/warm_absa_for_event.py`
- [x] **F7** · 影响指数算法可解释面板：`dataAdapter.explainImpact()` 给出 4 行人话拆解（来源·排名 / 基础分 / 平台热度加权 / 合计）；NewsCard heat badge + row 数字 hover 显示。验证 tooltip：`微博 · 第 1 名 / 基础分 91 / 热度值 0 → +0.0 / 合计 91/99`
- [x] **UI-1** · 侧栏 8 热榜分组折叠（2026-05-18 ✅）：`AppSidebar.vue` 加入可折叠「平台热榜 (8)」父节点，默认收起；chevron 始终 chevron-right + `.rotated` 类用 CSS transform 280ms cubic-bezier(0.2,0.8,0.2,1) 旋转，避免 icon 切换闪烁；body 用 grid-template-rows 1fr/0fr 过渡平滑展开。
- [x] **UI-2** · 热榜 #01 杂志头版排版升级（2026-05-18 ✅ commit `683b65d`）：`NewsCard.vue` lead 变体右侧 220px 加「前 5 影响指数」 mini bar（5 个竖直柱 + min-max 归一化放大对比 + 赤陶红→暖灰色阶）；major 变体 #02 加 3px 深赤陶红顶条、#03 加 3px 中暖橙顶条，与序号字色色阶呼应；#04/#05 保持素面。遵守 MASTER.md 克制原则（赤陶红不超 5 处、无渐变、无 hero）。
- [x] **UI-3** · 标题排版微调（2026-05-18 ✅ commit `a7c8acb` + 部分回退）：NewsCard `.card-title` 加 `text-wrap: balance`，消除 02-05 卡“末行 2-3 字孤行”（例：「贵族水果价格降」之后几张卡从 8/2 → 8/8 平衡）。**回退**：AppHeader `currentSourceMeta` kicker 副标题（`BAIDU REAL-TIME · 实时搜索趋势`）被评为同义重复装饰，与主标题「百度热搜榜」语义重叠、对用户无价值，已移除（sourceMetaMap 一并删除）。学到：**kicker 必须携带主标题之外的信息增量，不能只是多语言装饰**。

#### 第三梯队 · 工程化收口

- [x] ~~**J**~~ · 后端 rank=99 占位：实际已在 J/K 合并修复中走 `setdefault("rank", idx)` 兜底，无需改 nullable 策略
- [x] **K** · 全平台 listIndex 兜底迁移：`base.py:_save_to_db` 入口 enumerate 注入 rank，覆盖 baidu/bilibili/toutiao/zhihu 4 个未塞 rank 的 source；前端 `recomputeImpactByGroup` 保留作为防御层 + 搜索页相关度近似
- [ ] **L** · X1 Prometheus（旧 X1）：`/metrics` 暴露抓取耗时 / 事件数 / Agent 工具命中率 / LLM token 用量

### 推进顺序建议

1. ~~先把第一梯队 Batch VI-X 全部交付~~ ✅ 已清完
2. ~~再做 F7（可解释面板，强答辩点）~~ ✅ 已交付
3. ~~J/K 后端 rank 兜底~~ ✅ 已交付
4. ~~F6 ABSA 维度时间漂移~~ ✅ 已交付（2026-05-17）
5. ~~UI-1 侧栏 8 热榜分组折叠~~ ✅ 已交付（2026-05-18）
6. ~~UI-2 热榜 #01 杂志头版排版（lead mini bar + 顶条色阶）~~ ✅ 已交付（2026-05-18 `683b65d`）
7. ~~UI-3 标题 text-wrap balance~~ ✅ 已交付（2026-05-18 `a7c8acb`），kicker 副标题部分回退
8. **下一阶段 UI-4**：首页惊艳度。思路：**不**加装饰性 stat strip / hero / kicker，走「加信息增量」路线。候选：
   - **UI-4a**：NewsCard lead 卡染染 trend mini chart（32 点 sparkline柖量线）表现「该事件近 24h 发文趋势」 —— 需后端提供 /api/articles/{id}/publish_timeline
   - **UI-4b**：MoreHeadlines 添加「跨平台热度云图」迷你热力图 —— 现有数据（event 跨平台重合率）可直接利用
   - **UI-4c**：首页顶部加一个「状态轴」区（8 个热榜同步状态灯，实时点亮闪动）—— 让页面接入实时感
   - **选 b 最低风险**，复用现有 event/事件数据；选 a 需验证后端接口；选 c 需接 WebSocket 或轮询
9. F4/F5/F6 视答辩窗口期取舍

> 记忆要点：每改一步浏览器走查 + 单文件 commit message 用 `feat(batch-X): xxx` / `feat(f-N): xxx` 前缀，方便后续回溯。

---

## 最近交付节点（追加式日志）

> 本节统一收口"日度总结 / 验证报告"类临时文档。以后不再新建独立 `YYYY-MM-DD-总结.md`，所有交付节点追加在这里。

### 2026-05-18 · UI 热榜排版杂志化升级

**Commit 链**：`47df76f` → `2a03b86` → `683b65d`

**交付项**：
- **侧栏热榜折叠**（[AppSidebar.vue](yujing-ui/src/components/AppSidebar.vue)）
  - 8 个热榜收约到「平台热榜 (8)」可折叠父节点下，默认收起，依赖路由激活才展开
  - chevron 始终渲染 `mdi:chevron-right`，通过 `.rotated` 类控制 0° ↔ 90° 转动，transition `transform 280ms cubic-bezier(0.2,0.8,0.2,1)`（避免两个 icon v-if 切换时闪烁）
  - body 用 `grid-template-rows: 1fr ↔ 0fr` + opacity 双轨过渡，平滑展开无跳动
- **热榜 #01 lead 卡：右侧 mini bar 对比**（[NewsCard.vue](yujing-ui/src/components/NewsCard.vue) + [App.vue](yujing-ui/src/App.vue)）
  - lead 变体 body 改 grid `1fr / 220px`，kicker 跨满贯顶，main 在左、sparkline 在右
  - 5 个竖直柱录入前 5 名 impactScore：色阶 `#B45309 → #C77B2A → #D9A471 → #C9BFA8 → #A8A29E`，第 1 名（自身）为 accent 主色 + score/序号走 accent色
  - **柱高用 min-max 归一化放大差距**：18% 保底 + 82% 范围，避免 93/91/90 几乎等高
  - 768px 以下隐藏 sparkline，main 占满
- **热榜 #02/#03 major 卡：顶部色阶条**（[NewsCard.vue](yujing-ui/src/components/NewsCard.vue)）
  - #02 加 `border-top: 3px solid #92400E`（深赤陶红）
  - #03 加 `border-top: 3px solid #C77B2A`（中暖橙）
  - #04/#05 无顶条，与序号字色色阶（01 accent / 02 深赤 / 03 黑 / 04-05 暖灰）对齐呼应

**设计决策**：中途尝试过 hotlist-meta 元信息条（显示总条数 / 近 24h 增量 / 更新时间），用户反馈「有点假」后 `git reset --hard` 回退（commit `3d17813` 未推送，安全）。记取：**不要为了填空间加伪指标**，译了「克制、留白」原则（MASTER.md）。

**浏览器验证**：百度热搜榜 #01「贵族水果」右侧 sparkline 呈递减高柱（93/91/90/88/86）、色阶平滑；#02 深赤顶条 + #03 暖橙顶条渲染正常；#04/#05 保持素面不变。侧栏热榜分组默认收起，点击后 chevron 90° 平滑旋转、子列表从 0fr 动画展开到 1fr。

**备份**：`backup/before-hotlist-ui-2026-05` tag 指向 commit `47df76f`，任何回退可 `git reset --hard backup/before-hotlist-ui-2026-05`。

**UI-3 补充**（commit `a7c8acb` + 部分回退）：
- ✅ 保留：NewsCard `.card-title` 加 `text-wrap: balance`——消除 02-05 卡「末行 2-3 字孤行」
- ❌ 回退：AppHeader `currentSourceMeta` kicker 副标题（`BAIDU REAL-TIME · 实时搜索趋势`）。用户反馈「这个子有点多余，感觉没什么用」—— 「百度热搜榜」+「实时搜索趋势」同义重复，未带来信息增量。学到：**kicker 必须携带主标题之外的信息增量**（如「上次同步 12:34 · 该榜 100 条」这种实际状态），不能只是英文装饰。后续 UI-4 阶段不重走装饰路线。

---

### 2026-05-18 · UI-4c 首页 8 平台同步状态轴（❌ 已回退）

**用户反馈**：「这个状态轴还是好奇怪呀，不好看」→ 立即回退前端。

**重复教训**（第 2 次顶部指标条翻车）：
- 第 1 次：hotlist-meta（commit `3d17813` reset） — 用户「有点假」
- 第 2 次：PlatformStatusBar（本次回退） — 用户「奇怪、不好看」
- **结论**：「在 AppHeader 下方加一条横向指标条」这个**位置 + 形态**本身就违反 MASTER.md 的克制路线，**不论填的是真信息还是假信息都不行**。后续 UI-4 阶段不再尝试任何「顶部信息条」形态。

**保留 / 回退状态**：
- ❌ 前端：删除 `yujing-ui/src/components/PlatformStatusBar.vue` + App.vue 撤回 import / state / 轮询 / 模板插入
- ✅ 后端：保留 [app/api/routes.py](app/api/routes.py) `/api/sync/status` 的 `sources[]` + `meta` 扩展（向后兼容，未来若把信息塞入其他 UI 形态——如「全站同步」按钮内嵌进度——可直接复用，无须二次开发）

**下一次尝试方向**（候选，避开顶部条形态）：
- **A. 全站同步按钮内嵌进度**：点击「全站同步」按钮时按钮文字内 mono 计数器实时跳 `03 / 08 平台同步中`，按钮内部承载进度信息流，平时按钮空闲不占位
- **B. NewsCard 视口出现序列**：IntersectionObserver 触发文字行级渐现（每行 60ms 延迟），纯视觉杂志感升级，不加新信息
- **C. 暗黑/羊皮纸主题切换**：纸张色 ↔ 深石板色，280ms 过渡，给用户控制权而非"假精致"

---

### 2026-05-18 · UI-4A 全站同步按钮内嵌实时进度（✅ 已落地）

**用户选择 A 候选**：信息藏在已有交互里、不新增任何 UI 位置、复用 UI-4c 后端字段、彻底避开「顶部横向条」形态。

**实现要点**：
- [AppHeader.vue](yujing-ui/src/components/AppHeader.vue) 加 `syncProgress` prop（`{ done, total } | null`）。按钮文字三态：
  - `!loading` → 「全站同步」
  - `loading && syncProgress` → `<span class="mono tabular-nums">01</span> / <span class="mono">08</span> <span class="sync-label">平台·同步中</span>`
  - `loading && !syncProgress` → fallback「数据同步中」
- CSS 新增 `.sync-progress`（inline-flex baseline gap 4px）/ `.sync-num`（mono 700 tabular-nums，等宽防跳动）/ `.sync-sep`（opacity 0.55）/ `.sync-label`（11px opacity 0.78 letter-spacing 0.02em）
- [App.vue](yujing-ui/src/App.vue)：`handleRefresh` 启动同步时 `syncStartedAt = Date.now() - 5000`（5s 容差应对时钟漂移）+ `syncProgress.value = { done: 0, total: 8 }`；`pollSyncStatus` 每 2s 拉 `/api/sync/status`，比较 `sources[].latest_fetch_at >= syncStartedAt` 推算 done；同步结束时短暂显示完成态 1200ms 给视觉确认后清空 → 按钮回到「全站同步」
- **零后端改动**：完全复用 UI-4c 保留的 `sources[]` 字段

**Playwright 验证**：click `.btn-sync-all` → 立即变 `00/08 平台·同步中`（disabled + 自旋图标 + 灰底）→ 数秒内推进到 `01/08`（demo 数据轻量同步极快）→ 完成后回到「全站同步」。按钮宽度受 `tabular-nums` 锁定，数字递增无抖动。

**设计决策**：
- **信息密度藏在交互里**：平时按钮空闲，点击后才显现进度；零新增 UI 位置即满足「让页面接入实时感」需求
- **数字而非进度条**：`01/08` 比百分比/进度条更易扫读，符合"密集网格 + 表格化"克制美学
- **mono + tabular-nums 等宽**：防止 `01 → 08` 切换时按钮宽度跳动
- **5s 启动容差**：后端 sync 启动通常在 click 后 1-3s 内完成第一个 source，避免误判为「全部已完成」
- **完成态 1200ms 闪现**：给用户视觉确认而非瞬间消失（避免「我点了吗？」的疑惑）

**对比 UI-4c 收益**：同样的后端数据，不再需要顶部状态轴这块新 UI 空间，按钮原本就是「同步」语义所属位置，信息归位天然合理。

---

### 2026-05-18 · UI-5 热榜 01-05 卡片视觉强化（✅ 已落地）

**Commit**：`4ec8caf`（B+C 合并）

**问题排查**：浏览器走查发现 commit `683b65d` 加的「#02/#03 顶部色阶条」实际几乎看不见——`border-top: 3px` 经 Chrome sub-pixel 渲染为 2.6px + 锯齿后视觉只剩一条暗线；#04/#05 又完全无色条，5 档色阶名存实亡。同时 #01 头条与下方 major 卡区分度不够，仪式感弱。

**交付项**（[NewsCard.vue](yujing-ui/src/components/NewsCard.vue)）：
- **B. #01 头条卡仪式感升级**：
  - `.news-card--lead` 顶部 `border-top: 3px → 6px solid var(--color-accent)`，强化今日头条视觉锚点
  - 序号方块 `.card-rank-box::after` 右侧加 `content: "TOP STORY · 今日头条"` 衬线小 kicker（11px 700 letter-spacing 0.22em accent 色）
- **C. #02-#05 顶部色阶条强化**：
  - inline `majorTopBarStyle` 色带 3px → **5px**，避免 sub-pixel 渲染丢失
  - colors map 从 2 档（`1: #92400E, 2: #C77B2A`）补齐到 **4 档**（`1: #92400E 深赤陶红 / 2: #C77B2A 中暖橙 / 3: #D9A471 浅暖橙 / 4: #C9BFA8 暖灰`），让 #02-#05 形成完整色阶
  - major 卡序号 `.card-rank-box` font-family 从 `"Fira Code" mono` 改为 `var(--font-display, "Noto Serif SC", serif)`，letter-spacing -0.06em→-0.04em，与 lead 序号统一报纸杂志风

**设计决策**：
- 中途试过 hotlist-meta 元信息条（`总条数 / 24h 增量 / 更新时间`），用户反馈「有点假」立即 `git reset --hard HEAD~1` 回退（commit `3d17813` 未推送，安全）。**第 3 次重复教训**：「填空间用伪数据」是绝对禁忌（前两次：hotlist-meta、PlatformStatusBar）
- 结论沉淀：**视觉升级只能从既有元素的对比度 / 字体 / 间距入手**，绝不引入「为了好看而生」的元数据字段

**严格遵守 MASTER.md**：无 hero 大图、无渐变背景、无 hover 跳动、无 emoji icon、无 glassmorphism，仅在既有元素上做对比度与字体强化。

**Playwright 验证**：百度热搜榜可见 #01 6px 粗赤陶红 + "TOP STORY · 今日头条" 衬线小字；#02 深赤陶红 5px 粗带、#03 中暖橙、#04 浅暖橙、#05 暖灰，4 档色阶清晰可辨；02-05 序号全部宋体衬线，与 #01 序号呼应统一报纸风。

---

### 2026-05-18 · UI-6 全景事件页报纸编辑部化（✅ 已落地）

**Commit 链**：`e8c119f`（metric 数据栏）→ `3914d4b`（section-head）

**问题**：全景事件页面（侧栏「全景事件」入口）虽然是核心入口之一，视觉上还停留在「白底圆角小控件 + 黑体 + 糖果蓝/灰」的早期 SaaS 风，和热榜页的编辑部杂志风脱节。整体偏「工具感」，缺少惊艳度。

**交付项**（[App.vue](yujing-ui/src/App.vue)）：
- **metric 数据栏改造**（`.eh-metric-card`）—— 把「事件 5,411 / 热搜 6,550」从白底圆角小卡升级成报纸数据栏：
  - 背景 / 边框 / 圆角全去除，改 4px 左色带（首张 accent 赤陶红、第二张 border 灰，hover 时全变 accent）
  - 数字字号 `18px 900 黑体` → **`32px 700 衬线宋体 + tabular-nums + letter-spacing -0.03em`**
  - label 加衬线 + uppercase + letter-spacing 0.22em（报纸标签风）
  - icon 退到背景（opacity 0.7 → 0.4 灰色），让数字成为唯一视觉焦点
- **section-head 编辑部头版化**（`.section-head` + `.section-kicker`）—— 把「事件」分组标题从白底胶囊升级成报纸 section 头：
  - 去掉 20px 圆角 + 白底，改 **3px accent 顶条 + 1px hairline 底**
  - 新增 `.section-head-main` 容纳标题块（kicker + title 垂直叠加）
  - 标题上方加 `.section-kicker`：`EVENTS · 跨平台聚合`（11px 衬线 letter-spacing 0.22em accent 色）
  - 标题字号 20px → **26px 衬线宋体**

**设计语言统一**：现在全景事件页与热榜页用同一套视觉系统——
- accent 赤陶红色带（左 / 顶）
- 衬线宋体大字 + tabular-nums
- 衬线 letter-spacing 0.22em uppercase 小 kicker
- 报纸 hairline 分隔
- 克制的 icon（灰色 0.4 alpha 背景化）

**Playwright 验证**：全景事件页加载后顶部数据栏 `5,396 / 6,554` 视觉重心一下拉满；下方「事件」section 头 3px accent 顶条 + "EVENTS · 跨平台聚合" 衬线 kicker + 26px 衬线宋体标题，与热榜页 `TOP STORY · 今日头条` kicker 形成同源系列。

**虚惊一场记录**：编辑 `.section-head` 时切错页面到澎湃热榜，看到 `#01` 卡上方一条粗 accent 线以为是全局污染，实际是 NewsCard `--lead` 自带的 6px `border-top: solid var(--color-accent)`（UI-5 commit `4ec8caf` 的成果）。借此机会复盘：**全局未 scoped 的 class 名必须搜全工作区确认无冲突再改**，本次 `.section-head` 仅 App.vue 一处使用，安全。

---

### 2026-05-18 · 滚动条 + UI-7 翻页 + UI-8 头条仪式感（✅ 已落地）

**Commit 链**：`035ed1c`（滚动条）→ `ed13de5`（UI-7 翻页）→ `18866e4`（UI-8 EventCard lead）

**问题闭环**：用户反馈「展开平台热榜时，右边滚动条有点奇怪」+ 提出 3 项后续（翻页 / 编辑部全方面 / 头条仪式感）。本节归档前 2 项 + 滚动条临时插队。

**1. 滚动条报纸编辑部化**（[style.css](yujing-ui/src/style.css)）
原全局 `::-webkit-scrollbar` 用 `width: 10px` + 冷蓝灰 `rgba(100,116,139,0.38)` thumb + 蓝灰 track `rgba(226,232,240,0.46)` + 999px 圆角胶囊，与暖米色编辑部主题严重违和。改为：
- `width: 6px`（细一档）
- `thumb: rgba(180,83,9,0.22)`（accent 赤陶 22%），hover 加深到 42%
- `track: transparent`（融入米色底）
- 圆角 999px → 2px（去胶囊感）
- 同步加 firefox `scrollbar-width: thin; scrollbar-color`

**2. UI-7 More Headlines 翻页**（[App.vue](yujing-ui/src/App.vue)）
热榜 #6+ 原一次性渲染 80+ 条，首屏滚到底很远且节奏单一。改造：
- 新增 `rankingPage` ref + `RANKING_PAGE_SIZE = 10` 常量
- `pagedRankingArticles` / `rankingPageCount` / `visibleRankingPages` / `goToRankingPage` 全套 computed + 函数
- **复用** `.eh-pagination-wrapper` 报纸编辑部分页器样式（无新增 CSS）
- 序号正确递推：`(rankingPage - 1) * 10 + idx + 5`
- 切换平台 `watch(activePlatform)` 自动重置回第 1 页
- 翻页后 smooth `scrollIntoView` 到 `.ranking-rows` 顶部

**Playwright 验证**：百度热搜 page 1 显示 #06-15、page 2 显示 #16-25，分页器 `1 2 3 4 5 … 10` 完整、当前页 accent 圆角高亮。

**3. UI-8 EventCard 头条仪式感**（[EventCard.vue](yujing-ui/src/components/EventCard.vue) + [App.vue](yujing-ui/src/App.vue)）
全景事件页第 1 张事件卡和后续同款，缺少视觉锚点。复用 NewsCard `lead` 模式：
- EventCard 新增 `variant` prop（`default` / `lead`）
- lead 模板：`.event-main` 上方加 `.event-kicker`：**TOP EVENT · 跨平台焦点**（11px 衬线 0.22em uppercase accent）
- lead CSS：**6px accent 顶条** + 圆角去 + min-height 220 + padding 26×28 + title `clamp(22px, 1.8vw, 28px)` 衬线宋体（3 行 clamp）+ preview 14px 1.7 行高
- App.vue 两处 EventCard 调用（搜索 / 默认）都传 `:variant="idx === 0 ? 'lead' : 'default'"`

**全景事件三层赤陶红视觉系列形成**：
| 层 | accent 强度 | 元素 |
| --- | --- | --- |
| 数据栏 | 4px 左色带 | `.eh-metric-card:first-child` |
| section 头 | 3px 顶条 | `.section-head` + `.section-kicker` |
| TOP EVENT 头条 | 6px 顶条 | `.event-card--lead` + `.event-kicker` |

与热榜页 NewsCard `TOP STORY · 今日头条`（6px 顶条）同源 lead 模式呼应，全站编辑部杂志风视觉统一基本成型。

**待办**（用户提出待排期）：**编辑部风全方面升级**——搜索栏胶囊纸条化 / `.eh-pill-btn` 时间筛选下划线 tab 化 / `.eh-command-bar` 透明 hairline / AppHeader 报头 date kicker / 弹层 daisyui badge 衬线化。

---

### 2026-05-18 · 编辑部全面升级 step 1~4（UI-9 → UI-12，✅ 已落地）

**Commit 链**：`b2bcdcb`（UI-9）→ `3afd3d8`（UI-10）→ `4d7e7b5`（UI-11）→ `2c404df`（UI-12）

**目标**：把上一节"待办"的 5 条按"独立可见 → 同区域累进"顺序落地，本轮完成 4 条；剩 1 条 AppHeader 报头日期 kicker，按用户决定**不做**（无信息增量，参考 UI-3 学到的"kicker 必须有信息增量"原则）。

**1. UI-9 搜索栏胶囊→纸条**（[App.vue](yujing-ui/src/App.vue) `.eh-search-bar-group`）
- `border-radius: 100px` → `4px`（去胶囊感）
- border `rgba(148,163,184,0.24)` 蓝灰 → `var(--color-border)` 暖灰
- `box-shadow` 去掉（hairline 自承担存在感）
- focus-within 蓝色 shadow → `inset 0 0 0 1px var(--color-accent)`（双层 accent 边）
- `.eh-search-icon` 硬编码 `#94a3b8` → `var(--color-text-3)`

**2. UI-10 时间筛选 pill→下划线 tab**（`.eh-btn-group` / `.eh-pill-btn`）
原 daisyui `join` 圆角按钮组 + 白底容器 + active 态低透明 accent 背景，是典型 SaaS pill：
- `.eh-btn-group` 白底 padding + 12px 圆角容器 → 透明无 padding 横排 `gap: 22px`
- `.eh-pill-btn` 圆角 4px + 背景色 + padding-inline → 0 圆角 + 透明背景 + `border-bottom: 2px transparent`
- font 11px 800 → 12px 600 衬线 letter-spacing 0.12em（与 kicker 调性统一）
- inactive color `text-2` → `text-3` 暖灰
- active：透明背景 + accent 文字色 + accent 下划线（去掉所有背景填充）
- hover 加 hairline 下划线

**3. UI-11 `.eh-command-bar` 灰底圆角容器透明化**（`.event-hub-header` + `.eh-command-bar` + `.eh-cluster-label`）
原 `.event-hub-header` 套 daisyui `card bg-base-100 hs-panel` 三层白底圆角阴影 + `.eh-command-bar` 自身另有蓝灰 20px 圆角容器，toolbar 区域呈现 SaaS 浮卡感：
- 模板：移除 `.event-hub-header` 上的 `hs-panel card bg-base-100` 类（保留 `overflow-visible`）
- `.event-hub-header` padding 24px → 0；background/border/shadow 全部清零
- `.eh-command-bar` 浅蓝灰 `rgba(248,250,252,0.5)` 圆角 20px → 透明 + 顶/底 hairline 报头分隔
- `.eh-cluster-label` 11px 衬线 letter-spacing 0.18em uppercase（与 kicker 统一）

**toolbar 三件套统一收尾**：搜索栏纸条（UI-9） + 数据栏赤陶红色带（UI-6） + 下划线 tab（UI-10），三者共享同一张纸面，彻底无浮卡。

**4. UI-12 NewsCard daisyui badge 圆胶囊→衬线标签**（[NewsCard.vue](yujing-ui/src/components/NewsCard.vue)）
原 NewsCard 内 daisyui `.badge` 999px 圆胶囊 + `#f8fafc` 浅蓝灰底 + 800 字重，是头条/微博/百度/哔哩/知乎/澎湃/华尔街/财联社 **8 个平台页右上角统一显示的 SaaS 残留**：
- `.badge` 999px → 0 圆角，6px 10px → 2px 8px padding，font 衬线 600 + letter-spacing 0.06em
- `.badge.heat` 浅蓝灰底 + 边框 → 完全透明（heatStyle 已经管文字色，无需边框）
- `.badge.source` 保持 `surface-2` 暖灰底 + hairline 不变（来源标签需要弱底色区分）
- `.badge.cached` 保持 accent 浅底 + 边色不变（已采集仍需 accent 中性提示）
- `.card-badges` 文字 800 `#94a3b8` → 衬线 600 `var(--color-text-3)`

影响范围：NewsCard scoped，daisyui 全局 `.badge` 不受影响。

**今日累计 8 commit**（含 035ed1c / ed13de5 / 18866e4 / afdc0c4 / b2bcdcb / 3afd3d8 / 4d7e7b5 / 2c404df），全站编辑部统一基本完成。

**剩余可选**（明天再说，无强排期）：
- search-chip（NewsCard line 27 `badge badge-soft badge-primary`）reasons 豆子衬线化
- `event-empty-kicker` / `eh-search-meta` 空态提示衬线化
- 其他平台页（微博/知乎/哔哩）是否还有隐藏的软 SaaS 容器
- AppHeader 标题区域是否需要 date kicker（用户判断：**不做**，无信息增量）

---

### 2026-05-19 · 编辑部弹层 + C 深层数据组件（UI-14c / UI-15a-e / UI-16，✅ 已落地）

**Commit 链**：`37660a3`（UI-14c）→ `02f4545`（UI-15a）→ `8f02361`（UI-15b）→ `25c2dca`（UI-15c）→ `e178e44`（UI-15d）→ `dcdf134`（UI-15e）→ `7ac3f06`（UI-16）

**目标**：把昨日（v1.11）明确「明天再说」的剩余 3 块——订阅推荐分数 chip / 全部 5 个弹层 / 3 个深层数据组件——按可见度倒序刷完，回到全站零 SaaS 残留水位。本日 7 commit 跨 1 处订阅页深层 + 5 个弹层 + 3 个深层数据组件。

**1. UI-14c 订阅推荐分数 chip 衬线 hairline 化**（[SubscriptionPanel.vue](yujing-ui/src/components/SubscriptionPanel.vue) `.sub-rec-item .score-chip`）

v1.11 收尾时刻意保留的最后 3 个橙/蓝/红圆角 chip（匹配 8.97 / 订阅词 +5 / 兴趣 +3.97）—— 浅色填充底 999px 圆胶囊 → 透明 + 单色 hairline + 衬线 0.22em uppercase + tabular-nums，删 explainability 信息密度的同时收齐到编辑部 token。

**2. UI-15a EventModal + AbsaTimelineChart 弹层**（[EventModal.vue](yujing-ui/src/components/EventModal.vue) / [AbsaTimelineChart.vue](yujing-ui/src/components/AbsaTimelineChart.vue)）

事件详情弹层（用户最常见的弹层入口，按 ranking-row 触发）：
- modal-mask backdrop blur8 → 暖灰 rgba(28,25,23,0.42) 去 blur（弹层 blur 在 v1.11 被认为"合理收敛背景"，本轮直接全部撤掉以彻底统一）
- modal-shell 12px + 多层 shadow → 4px + 顶 3px accent border + 弱暖色 shadow
- title 蓝灰 sans 700 → 宋体 800 -0.01em + text-1；kicker 加 22em uppercase
- 内部 .timeline-row.tl-card / 文章列表 / 各类徽章全部 0 圆角 + token border + hover 仅 border accent（无 translateY/scale）
- 子组件 AbsaTimelineChart `.absa-timeline-badge` 因 scoped 隔离，必须改子组件自身样式才能生效

**3. UI-15b AnalysisModal 弹层**（[AnalysisModal.vue](yujing-ui/src/components/AnalysisModal.vue)）

事件深度分析弹层（intel 卡片 + AI 解读卡）：全部 12-24px 圆角 + 多层 shadow → 4px modal + 0 圆角内部卡片 + 顶 3px accent border + 弱 shadow + hover 仅 accent border。

**关键坑**：`.intel-box` 选择器在该文件内有**两处重复定义**（line 879 + line 1142），仅改第一处时被第二处 CSS 优先级相同地覆盖；computed style 仍是 24px shadow。修复方案是 `grep ^\.intel-box \{` 找出两处行号，分别加足够上下文 surgical replace。**学到**：长 .vue 文件改样式前必须 `grep ^\.selector \{` 检查是否有重复定义。

**4. UI-15c BriefModal 弹层**（[BriefModal.vue](yujing-ui/src/components/BriefModal.vue)）

AI 早报详情弹层（PDF/Word/PPT 三按钮共享）：整体走 0 圆角 + accent hairline icon "shield-key" 直角框 + "YUJING · MORNING BRIEF" 11px 衬线 0.22em uppercase kicker + 宋体大标题 + 黑色 0 圆角衬线 uppercase 主按钮。**浏览器实拍验证通过**（首页 → AI 助手 → 早报 banner → 详情）。

**5. UI-15d TopicModal 弹层**（[TopicModal.vue](yujing-ui/src/components/TopicModal.vue)）

主题深度阅读弹层（pipeline 卡 + insight 侧栏 + markdown 文章）：modal-mask / topic-main-title / quick-entry-card / pipeline-card（加 3px accent 左色带 hover）/ insight-card / sidebar-tabs active / markdown h1-h3 全部 0 圆角 + token + 宋体 + 衬线 kicker。

**实拍限制**：`openTopicDetail` 在 App.vue line 1237 仅声明无调用方，全工程无 UI 触发入口；`TopicCard` 也只在 NewsCard 注释里被提及。本次仅基于规则提交，commit message 注明此限制。

**6. UI-15e CredentialModal 弹层**（[CredentialModal.vue](yujing-ui/src/components/CredentialModal.vue)）

凭据配置弹层（侧栏底部"凭据资产配置"按钮触发）：
- overlay blur16 → 暖灰 alpha 去 blur
- box 6px → 4px + 顶 3px accent border + 弱暖色 shadow
- title-icon 米色块 → 透明 + accent hairline 直角图标框
- field-label 灰 0.04em → accent 22em 衬线 uppercase 10px
- cred-select / status-card / textarea / feedback / btn 全部 0 圆角
- status-card.active 浅绿底 → 透明 + 35% alpha 绿 border
- feedback success/error/info 全部透明 + 左 3px 色带
- btn-primary brand 蓝 → text-1 黑，hover accent，衬线 18em uppercase

**浏览器实拍验证通过**（reload → 侧栏"凭据资产配置"→ `.credential-box` count=1 → screenshot）。

**7. UI-16 C 深层数据组件 CSS 一致化**（[SearchInsightChart.vue](yujing-ui/src/components/SearchInsightChart.vue) / [SourcePulse.vue](yujing-ui/src/components/SourcePulse.vue) / [CompareDashboard.vue](yujing-ui/src/components/CompareDashboard.vue)）

三件深层组件以 1 commit 合并刷完，规则与上述弹层一致（容器 0 圆角 + 顶 3px accent + hairline + 内部卡片 0 圆角 / metric label 衬线 22em uppercase / hover 仅 border accent / 999px 胶囊 → 0 / 蓝渐变 → token surface）。

**实拍限制**：
- `SearchInsightChart` 在 App.vue line 1523 被注释禁用
- `SourcePulse` 全工程无 `<SourcePulse` 使用方（属未挂载历史组件）
- `CompareDashboard` 仅在 AIConsultant 对话流"深度对比"节点出现，实拍成本高

本次仅做 CSS 一致化，避免未来挂载/启用时风格断层。

**今日累计 7 commit**（37660a3 / 02f4545 / 8f02361 / 25c2dca / e178e44 / dcdf134 / 7ac3f06）。

**已覆盖范围**（v1.10 + v1.11 + v1.12 累加）：
- ✅ 全局滚动条 + AppHeader 顶栏 + 8 个平台热榜 NewsCard
- ✅ 全景事件页（toolbar + EventCard lead + 数据栏 + section-head + 翻页）
- ✅ AI 助手页 100% + 我的订阅页 100%
- ✅ 弹层组 5/5（EventModal + AnalysisModal + BriefModal + TopicModal + CredentialModal）
- ✅ C 深层数据组件 3/3（SearchInsightChart + SourcePulse + CompareDashboard，CSS 一致化未实拍）
- ✅ 子图组件 AbsaTimelineChart（随 UI-15a 一并刷）

**剩余可选**（无强排期）：AgentTrace / VisualWidget / TopicCard / EventCard 内部碎片（如果浏览器实拍发现仍有蓝渐变/圆胶囊残留再单独补刀）。

**8. UI-17 AI 助手 composer 输入框对比度修复**（[AIConsultant.vue](yujing-ui/src/components/AIConsultant.vue) `.composer-shell` / `.mcp-input-area` / `.composer-meta`，commit `972a15a`）

v1.12 文档发布后用户反馈"AI 助手对话框看不清"。根因排查：

- `composer-shell { background: transparent }`（v1.11 UI-13 改的）在右侧米色聊天流上几乎无视觉边界
- `mcp-input-area { background: transparent }` 同样透明，没有"纸条"层级
- textarea 未声明 `color` / placeholder color，使用浏览器默认 `#757575` 灰色叠加米色背景对比度严重不足

修复（4 处替换）：

- `composer-shell.background`: `transparent` → `var(--color-surface-2)` (#F5F5F2) — 整条输入区与聊天流形成弱分层
- `mcp-input-area.background`: `transparent` → `var(--color-surface)` (#fff) — 输入框显形为一张白色纸条
- `mcp-input-area`: 新增 `color: var(--color-text-1); line-height: 1.6` — 正文色显式声明
- `mcp-input-area::placeholder`: `var(--color-text-3) + opacity: 1` — 显式覆盖浏览器默认，确保 placeholder 仍是暖灰但可读
- `composer-meta.color`: `#94a3b8` → `var(--color-text-3)` — 提示文字归位到 token

**浏览器实拍验证通过**（reload → AI 助手页 → composer 空态 + 输入态对比度均达标）。

**学到**：编辑部"减法"做得太狠会丢可读性 —— **透明 ≠ 编辑部，hairline + 纸面层级才是**。下次做"全透明"减法前必须问一句"文字仍能清晰对比背景吗"，必要时先放层 surface-2 再决定要不要继续减。

**学到**：
1. .vue 长文件改样式必须先 `grep ^\.selector \{` 检查重复定义（UI-15b `.intel-box` 两处定义被第二处覆盖的坑）
2. scoped 样式无法穿透子组件 → 子组件 badge 类必须改子组件自身（UI-15a `.absa-timeline-badge`）
3. 不是所有组件都有 UI 入口 —— 找不到入口的组件提交时必须在 message 里注明"无 UI 入口未验证"，绝不瞎编截图说"已验证"
4. v1.11 收尾时主动留 explainability chip（分数 chip）以「保留信息密度」为理由，但实际上衬线 hairline 化（v1.12 UI-14c）后信息密度反而更清晰 —— **不要为保留装饰找借口**
5. **"全透明"减法是 v1.11 的过度减法**（UI-17 composer 透明导致用户感知"看不清"），透明不等于编辑部，hairline + 纸面层级才是

**9. UI-18 CompareDashboard 窄气泡塌列修复**（[CompareDashboard.vue](yujing-ui/src/components/CompareDashboard.vue) `.radar-side`，commit `942b758`）

UI-16 完成时因 SearchInsightChart/SourcePulse 无 UI 入口、CompareDashboard 仅 AIConsultant 深处出现，未做实拍。本日「继续验证」环节进入历史会话「用雷达图对比微博、知乎、头条、百度、B站这五个平台的舆情画像」，实拍发现 `.radar-side` 的 `grid-template-columns: minmax(0,1fr) minmax(0,1fr)` 在 AI 助手消息气泡（~600px）内被瓜分到每列仅 ~125px，导致：

- leader-list 5 行（情报量/事件覆盖/...）的「领先平台名」漂浮覆盖右栏
- radar-raw 4 列表头被压成单字竖排（事/件、正/向）
- 数据行 "94.9 / 100 / 1.3 / 50 / 6" 错位

`@media (max-width: 520px)` 是 **viewport 媒体查询**，在桌面端 1500+ viewport 不会触发，治标不治本。

**修复**：直接把 `.radar-side` 从 2 列 grid 改为单列 flex（leader-block 在上 + raw-block 在下），让两块数据各占满宽度。这种「单列垂直堆叠」在窄气泡里反而比 2 列瓜分更可读。**浏览器实拍验证通过**。

**学到**：viewport 媒体查询 ≠ 容器查询。组件被放进窄容器时不能依赖 `@media (max-width)` 决定塌单列；如果不上 container queries（@container），就应该默认就是单列垂直堆叠，让宽屏自然撑开即可。

**10. 全站同步「卡 0/8 同步中」根因排查 + 日志落盘**（[app/api/routes.py](app/api/routes.py) `sync_trigger_crawlers`，commit `b86af58`）

用户反馈点「全站同步」按钮后会卡很久显示「0/8 平台同步中」。复现：用 PowerShell 触发 `/api/articles?force_refresh=true` 并每 2s 轮询 `/api/sync/status`，看到 38 秒内 done 一直 0/8，最后 fetching=false 时仍是 0/8（latest_fetch_at 没推进）。

**根因**：8 个 crawler 同步抓取里只要全部命中临时网络/反爬抖动 → `asyncio.gather(..., return_exceptions=True)` 把所有异常**静默吞掉** → 8 个源的 `fetch_time` 都不更新 → 前端 `done = sources.filter(s => s.latest_fetch_at >= syncStartedAt)` 永远是 0 → 用户感知是「同步中」卡 ~100s（_run_refresh_job 还要跑 `_refresh_events_cache`）。

**修复**：把 `gather(return_exceptions=True)` 改为对每个 crawler 包 try/except 返回 `("ok"|"err", name, duration, payload)` 三元组，逐个 print + 落盘到 [logs/sync_debug.log](logs/sync_debug.log)：

```
[17:04:20] [同步] OK WeiboHotSearch (3.27s)
[17:04:20] [同步] OK BaiduHotSearch (1.55s)
[17:04:20] [同步] OK ToutiaoHotBoard (15.41s)
...
[17:04:20] [同步] 汇总: 8/8 成功
```

下次再卡 0/8 时可立刻读 `logs/sync_debug.log` 看是哪个 crawler 失败、失败类型是什么。**潜在改进**（未做）：给每个 crawler 加 30s 超时保护，避免单个 socket 挂死拖死整个 job。

**学到**：`asyncio.gather(return_exceptions=True)` 必须搭配「逐个 inspect 结果 + 打 log」否则等于把所有异常永远 silent。本次「卡 0/8」之所以拖了几个版本才发现，就是因为日志一片寂静，看后端控制台和数据库都看不出端倪。

**11. per-crawler 30s 超时保护 + 前端同步失败 toast**（commit `00a3bfa`）

第 10 条修复只解决了「异常被吞」问题，但**没处理单个 crawler socket 挂死拖死整个 job 的风险**（例如某平台临时反爬返回 200 但 stream 不闭合）。用户反馈「还是会卡」后補射2块：

**后端**：`asyncio.wait_for(c.run_and_save(), timeout=30.0)` 包 per-crawler。调研值最慢 ToutiaoHotBoard ~15s，30s 留 2x 余量。超时走 `"timeout"` 分支写入 `swr_cache["last_sync_errors"]`（结构 `{name, reason: "timeout"|exc_repr, duration}`）与 `last_sync_finished_at` 一起返回 `/sync/status`。

**前端**：`pollSyncStatus` 收尾检查 `last_sync_errors.length > 0 && finished_at >= syncStartedAt` 时弹 toast：editorial 风 **黑底 #1c1917 + 3px accent #B45309 左色带 + 0 圆角 + 13px** 与全站一致（未复用 AIConsultant 里那个 SaaS 蓝/黄/红 + 10px 圆角的旧 toast，避免违反 token）。文案示例：`同步 6/8 完成，知乎热榜、B站热门 失败（含超时）`。成功 8/8 不打扰（设计意图）。

**实拍验证**：8/8 全部成功路径最慢 14.14s 远低于 30s；DOM 注入验证 toast 视觉与编辑部调性一致。

**学到**：复用现有 toast 函数看似省事，但 v1.11 留下的 SaaS 色 toast（蓝 #0EA5E9 / 黄 #F59E0B / 红 #EF4444 + 10px 圆角）与 v1.12 编辑部色系冲突 —— **联调后变量环境下，「都是 UI 都叫 toast」不意味着可以复用**，还是重写一版更保险。

**12. /sync/status 永远转圈终极修复 — 装饰器误绑 + 同步 DB 阻塞双 bug**（commit `546b986`）

第 10、11 条把后端做厚之后，用户反馈「为什么现在全站同步的功能还是不能用啊，还是会一直转圈卡住啊，你有看过日志吗」。读 `logs/sync_debug.log` + 手测 `/api/sync/status` 发现两个独立的 bug 互相掩盖：

**Bug A · 装饰器误绑同步函数**：第 11 条修复时为引入 `_query_sync_status_db_snapshot` 同步辅助函数（用于 `to_thread` 包装），曾 `str_replace` 把 `async def get_sync_status` 替换为 `def _query_sync_status_db_snapshot`，但**未把上方紧邻的 `@router.get("/sync/status")` 装饰器一起包进 oldString**。结果 FastAPI 在同一 path 注册了两个端点：装饰器先绑到同步辅助函数（直接返回 `(sources, meta)` tuple → 序列化成 JSON list `[[...], {...}]`），后面真正的 `async def get_sync_status()` 装饰器也存在但被 FastAPI 「先注册先胜出」覆盖。**前端 `data.fetching` 永远是 `undefined`，pollSyncStatus 不会停 → 永远转圈**。修：删多余装饰器，让 `_query_sync_status_db_snapshot` 单纯做同步函数。

**Bug B · async 端点裸跑同步 ORM 阻塞事件循环**：原 `get_sync_status` 是 `async def` 但内部直接 `db = SessionLocal()` 跑 4 个 `db.query(...).group_by(...)` —— 同步 SQLAlchemy 调用在 async 函数里**会阻塞 event loop**；同时 `_refresh_events_cache` 的 SQLite 重 SQL 也在跑，两者抢同一把 SQLite 写锁，导致 status 端点 5s 超时（前端 axios 默认）。**修**：用 `await asyncio.to_thread(_query_sync_status_db_snapshot)` 把 4 个 query 丢线程池，同时加 `_status_snapshot_cache + _status_snapshot_lock`（fetching 时 TTL 2.0s、空闲 0.5s）减少 DB 压力。

**实测验证**（terminal 30 次轮询）：
- 空闲态 status：75~621ms 返回正确 dict ✓
- 触发态 status：75~1800ms 全部成功（修前必 5s 超时）✓
- 端到端 1 次全站同步：17.19s sync（Toutiao 最慢）+ 30s `_refresh_events_cache` = 47s 总 fetching=True → 干净归 False ✓

**浏览器实拍**（pageId `874e5d37`）：点「全站同步」→ 按钮立即变 `00 / 08 平台·同步中` disabled → 25s 后推进到 `01 / 08` → 55s 后按钮恢复「全站同步」。8/8 全成功不弹 toast（设计意图）。**前两版补丁都没修好这个 bug 的根因**，靠的是用户「你有看过日志吗」的质问倒逼读 `sync_debug.log` + 直接 curl 端点看 raw 返回，才暴露装饰器误绑的问题。

**学到**（已写入 `/memories/repo/yujing-workflow.md`）：
1. `str_replace` 修改函数签名时**必须把上方紧邻的装饰器一起包进 oldString**，否则装饰器会留在原位绑到新签名上。
2. FastAPI `async def` 端点里**禁止裸 `db.query()`**，必须 `await asyncio.to_thread(...)` 包同步 ORM 调用。否则不仅阻塞 event loop，还会和后台同样跑 SQLite 的 task 抢锁，被外层 axios 5s 超时打死。
3. 「能否复现 bug」与「能否定位 bug」是两个能力 —— 复现靠 PowerShell 触发同步，定位必须读端点的 raw JSON 才能发现 `data.fetching=undefined` 这种结构错配。

---

### 2026-05-19 · 编辑部全站补刀 step 5~9（UI-13 → UI-14b，✅ 已落地）

**Commit 链**：`e4fb9a4`（UI-13）→ `9129ed0`（UI-13b）→ `e91f659`（克制为先 · 去 TOP STORY）→ `3539bea`（UI-13c）→ `79229b3`（UI-14a）→ `b2b03a4`（UI-14b）

**目标**：把昨日「明天再说」的两块重灾区（**AI 助手页** + **我的订阅页**）一次性刷完，并顺手清掉全站最显眼的 SaaS 残留（AppHeader 顶部 blur / event-toolbar 同色渐变浮卡 / 翻页 blur 胶囊）。本日 6 commit 跨 2 个页面 + 1 处全局头部 + 1 处全景页 toolbar 二次收尾 + 1 处「克制为先」减法。

**1. UI-13 AI 助手页主体编辑部化**（[AIConsultant.vue](yujing-ui/src/components/AIConsultant.vue)）

该页是用户日活最高的页面之一，但此前是**全站 SaaS 感最重的区域**：左侧会话栏白底浮卡 + 浅蓝灰 active + 黑胶囊新建按钮；下方 composer 白底 blur + 24px 圆角 + 多层 shadow；快捷动作浅蓝渐变；建议气泡黄底圆胶囊。本轮一次刷完：

- `.ai-shell` 浅蓝灰 → `var(--color-surface-2)` 暖灰底（与全站纸面统一）
- `.workspace-header` 白底 blur 浮卡 → 透明 + 底 hairline
- `.session-rail` 浮卡 → 透明 + 右 hairline 报头分隔；`.rail-head` daisyui `card` 移除 → 透明 + 顶/底 hairline
- `.session-create` 黑色 12px 圆胶囊 → 透明 + accent border + 衬线 letter-spacing 0.12em（编辑部"新建专栏"调性）
- `.session-card` 12px 圆角 + 白底 shadow → 0 圆角 + 暖灰；active 态白底 shadow → `surface-2` + 左侧 3px accent `::before` 色带（替换填充色用色带表达 active）
- `.composer-shell` / `.composer-panel` 白底 blur + 24px 圆角 + 多层 shadow → 透明 + 顶 hairline + 0 圆角；`.mcp-input-area` 12px → 4px hairline；`.btn-send-capsule` 12px → 0 圆角 + 衬线 letter-spacing
- `.quick-action-btn` 浅蓝渐变 12px → 透明 + hairline + 衬线
- `.suggest-area` header 黄色 → accent 色 + uppercase 0.22em kicker；`.suggest-pill` 黄底圆胶囊 → 0 圆角 + accent 左色带 + 衬线
- `.agent-badge` 999px 圆胶囊 → 0 + hairline + 衬线 letter-spacing 0.08em

**2. UI-13b AI 助手收尾**（同文件）

UI-13 截图复核后发现剩 2 处显眼残留：
- PDF / Word / PPT 三按钮还是 daisyui pill + 浅蓝灰底（早报 banner / 消息卡 / BriefModal 共享样式）→ 0 圆角 + 衬线 + accent hairline
- 「继续追问」TOOL_FOLLOW_UPS 豆子是黄底圆胶囊 → 透明 + accent border + 衬线
- `.suggest-header` 加 uppercase 0.22em 衬线 kicker，与 suggest-pill 改造收尾一致

**3. e91f659「克制为先」· 移除 NewsCard lead 头条 TOP STORY · 今日头条 kicker**（[NewsCard.vue](yujing-ui/src/components/NewsCard.vue)）

用户原话："主页的 TOPSTORY · 今日头条不要了，用处不大，我们不需要那么多花哨的东西"。复盘：lead variant 本身已经有 6px accent 顶条仪式感，再多一行 11px uppercase kicker 反而花哨重复；而且每个平台头条都加这一行后，8 个平台首页串起来形成视觉噪音。删除 `.news-card--lead .card-rank-box::after { content: "TOP STORY · 今日头条" }` 整块，保留 6px 顶条 + 序号 01 着色就够编辑部仪式感了。**学到**：仪式感不等于多堆装饰元素，"减法 ≥ 加法" 在编辑部风里同样成立。

**4. UI-13c 全站高可见度残留清理**（[AppHeader.vue](yujing-ui/src/components/AppHeader.vue) + [App.vue](yujing-ui/src/App.vue)）

顺着 e91f659 的「克制」原则做一次全站可见度排序扫描，找出 3 处仍然刺眼的 SaaS 残留：

- **AppHeader**（最致命，全局顶栏）：`rgba(255,255,255,0.82)` + `backdrop-filter: blur(24px)` 毛玻璃 → `var(--color-surface)` 纸白 + 底 1px hairline（毛玻璃在杂志风里彻底不属于）
- **全景事件 `.event-toolbar`**（昨日 UI-11 只清了 `.eh-command-bar` 容器，toolbar 内层还残留）：28px 大圆角 + 135deg 双色渐变 + 大 shadow → 4px + 纸白 + hairline + 无 shadow
- **`.toolbar-choice--active` / `--mode-active`**：同色渐变填充 + 蓝色 shadow → 纯 accent / brand 实色（去渐变去蓝光晕）
- **翻页按钮 `.eh-page-btn` / `.eh-page-active`**：blur 12px 胶囊 + 渐变 + 蓝色 shadow → hairline + 0 圆角 + transparent + active 用纯 accent

**5. UI-14a 我的订阅页编辑部化主体**（[SubscriptionPanel.vue](yujing-ui/src/components/SubscriptionPanel.vue)）

订阅页是用户日活第二高的页面，整页 3 大白底圆角浮卡（订阅词 / 黑名单 / 用户画像）+ kicker 浅 accent 圆胶囊 + 黑胶囊刷新按钮 + 浅灰 999px 数字徽章，**SaaS 感比 AI 助手页有过之而无不及**。一次刷完主体：

- `.sub-hero` border-radius 4 → 0；顶 accent 3px → 6px（与 TOP EVENT 头条同源仪式感）
- `.sub-kicker` 浅 accent 底 + 3px 圆角 → 透明 + accent border + 0 圆角 + 衬线 uppercase 0.22em
- `.sub-refresh-btn` 4 → 0 + 衬线 letter-spacing 0.12em + 去 translateY hover
- `.sub-card` 20px 大圆角 → 0 + 暖 hairline + 顶 1px accent 色带（与 NewsCard 02 同源色阶）
- `.sub-card-head` 加 uppercase letter-spacing 0.22em + 衬线
- `.sub-count` 999px 浅灰圆胶囊 → 透明 + 衬线 + tabular-nums（数字编辑部最关键的字号变化）
- `.sub-select` / `.sub-input` 10 → 4 + 暖 hairline
- `.sub-btn` 4 → 0 + 衬线 0.12em；primary / danger 去 translateY hover

**6. UI-14b 我的订阅页残留**（同文件）

UI-14a 截图后剩 4 处可见残留，一次刷完：

- `.sub-chip-kind`（关键词条目内的「关键词」label chip）浅 accent 圆胶囊 → 透明 + accent border + uppercase 衬线
- `.sub-tag`（常看数据源 TOP 8 个浅蓝灰圆胶囊）→ 透明 + hairline + 数字加粗 tabular-nums
- `.sub-profile-label` kicker（常看数据源 TOP / 兴趣标签）→ uppercase letter-spacing 0.22em 衬线
- `.sub-recommend` / `.sub-rec-list` 推荐区白底 20px 浮卡 → 0 圆角 + hairline；`.sub-rec-item` 推荐条目 14px 圆角 + hover 蓝光晕 → 0 + accent hairline + hover 仅左色带
- `.sub-dismiss-restore` 忽略撤销小胶囊 → 0 圆角

**今日累计 6 commit**（e4fb9a4 / 9129ed0 / e91f659 / 3539bea / 79229b3 / b2b03a4），AI 助手 + 我的订阅 两大日活页全部编辑部化收口。

**已覆盖范围**：AI 助手页 100% / 我的订阅页 100% / AppHeader 顶部全局 / 全景事件 toolbar 二次清理 / 翻页按钮 / NewsCard lead 克制减法

**剩余可选**（明天再说，无强排期）：
- `.sub-rec-item` 内推荐分数 chip（匹配 8.97 / 订阅词 +5 / 兴趣 +3.97 三种橙/蓝/红底圆角分数 chip）—— 删了反而丢 explainability 信息密度，可作 UI-14c 衬线 hairline 化
- 弹层组（EventModal / AnalysisModal / BriefModal / TopicModal / CredentialModal）—— 弹层 blur 在视觉上属合理收敛背景，用户也未直接接触
- 深层数据组件 CompareDashboard / SourcePulse / SearchInsightChart / AbsaTimelineChart —— 可见度低

**学到**：
1. 「全站编辑部化」可以按页面日活倒序刷，AI 助手 + 订阅页两块刷完后整站 SaaS 残感几乎清零
2. 「减法 ≥ 加法」：e91f659 删除一行 kicker 文字比添加任何装饰都更编辑部
3. 高可见度优先级排序：**全局顶栏（AppHeader）> 主体页 toolbar > 卡片内部 chip** ——按这个顺序刷收益最大

---

### 2026-05-18 · F6 ABSA 后置修复（hash 一致性 + 自适应桶宽）

**Commit 链**：`78f2e46` → `f1f6334`

**修复项**（在 5-17 F6 主线交付后排查到的 2 个真实生产问题）：
- **hash 一致性 bug**：[app/services/absa_service.py](app/services/absa_service.py) 缓存 key `sha1(title + content)` 中 content 在写入 / 读取时经过不同的字符串归一化路径（写入是 LLM 入参 trim 后版本，读取是原始 article.content），导致同一文章二次访问全部 miss → 用户感知是「缓存形同虚设、每次开事件都重新跑 LLM」。修复：抽出 `_normalize_for_hash(text)` 统一归一化（`str(text or "").strip()`），写读两侧共用
- **bucket_hours 自适应**：原固定 `bucket_hours=12`，对短事件（< 24h）折线被压缩成单点；对长事件（> 7d）又过于稠密。修复：[app/api/routes.py](app/api/routes.py) `absa_timeline` 端点根据 `(max_pub - min_pub).total_seconds() / 3600` 自动选择 1/3/6/12/24h 桶宽（< 12h 用 1h、< 48h 用 3h、< 168h 用 6h、< 720h 用 12h、否则 24h）
- **诊断脚本**：[scripts/diagnose_absa_cache.py](scripts/diagnose_absa_cache.py) 答辩前批量校验缓存命中率，输出 `命中 / 总数 / 比率 / 缺失文章 hash 列表`

**回归**：以 commit `47df76f` 备份点为基准在沙盒事件上手动重跑 1 次 ABSA → 第 2 次访问从「全部 miss」→「全部 hit」，curl `/api/events/.../absa_timeline?` 响应从 30+s 降至 < 100ms。

---

### 2026-05-17 · F6 ABSA 维度时间漂移

**Commit 链**：`78f2e46` → `f1f6334`

**修复项**（在 5-17 F6 主线交付后排查到的 2 个真实生产问题）：
- **hash 一致性 bug**：[app/services/absa_service.py](app/services/absa_service.py) 缓存 key `sha1(title + content)` 中 content 在写入 / 读取时经过不同的字符串归一化路径（写入是 LLM 入参 trim 后版本，读取是原始 article.content），导致同一文章二次访问全部 miss → 用户感知是「缓存形同虚设、每次开事件都重新跑 LLM」。修复：抽出 `_normalize_for_hash(text)` 统一归一化（`str(text or "").strip()`），写读两侧共用
- **bucket_hours 自适应**：原固定 `bucket_hours=12`，对短事件（< 24h）折线被压缩成单点；对长事件（> 7d）又过于稠密。修复：[app/api/routes.py](app/api/routes.py) `absa_timeline` 端点根据 `(max_pub - min_pub).total_seconds() / 3600` 自动选择 1/3/6/12/24h 桶宽（< 12h 用 1h、< 48h 用 3h、< 168h 用 6h、< 720h 用 12h、否则 24h）
- **诊断脚本**：[scripts/diagnose_absa_cache.py](scripts/diagnose_absa_cache.py) 答辩前批量校验缓存命中率，输出 `命中 / 总数 / 比率 / 缺失文章 hash 列表`

**回归**：以 commit `47df76f` 备份点为基准在沙盒事件上手动重跑 1 次 ABSA → 第 2 次访问从「全部 miss」→「全部 hit」，curl `/api/events/.../absa_timeline?` 响应从 30+s 降至 < 100ms。

---

### 2026-05-17 · F6 ABSA 维度时间漂移

**Commit**：`feat(f6): ABSA 维度时间漂移图`（详见下次提交 hash）

**交付项**：
- **后端**：[app/api/routes.py](app/api/routes.py) 新增 `GET /api/events/{event_id}/absa_timeline?bucket_hours=12&top_k_aspects=5`
  - 只读 `runtime/absa_cache/<sha1(title+content)>.json` 已命中的文章，**不触发新 LLM**（避免大事件首访同步等待数十秒）
  - 极性数值化：positive=+1 / neutral=0 / negative=-1；桶内对同 aspect 取均值
  - 时间桶按文章 `pub_date`（缺失回退 `fetch_time`）对齐到 `bucket_hours` 整点
  - 返回 `coverage_ratio` 让前端可展示命中比例
- **前端**：新增 [yujing-ui/src/components/AbsaTimelineChart.vue](yujing-ui/src/components/AbsaTimelineChart.vue)
  - ECharts multi-line 6 色循环 + connectNulls=false 自动断线
  - 点大小∝√count（6-18px）；顶点 itemStyle 按情感染色（≥+0.34 绿 / ≤-0.34 红 / 中间灰）
  - 完善 4 态：loading / error / empty（带 hint 引导用户触发 AI 分析）/ "时间跨度不足"
  - tooltip 显示 `aspect · 极性值 · 证据数`
- **EventModal 接入**：[EventModal.vue](yujing-ui/src/components/EventModal.vue) 在"数据分析" section 后追加（保持纵向 section 风格，不引入 tab 改造）
- **预热脚本**：[scripts/warm_absa_for_event.py](scripts/warm_absa_for_event.py) 答辩前批量为指定事件生成 ABSA 缓存

**修复点**：组件 `loading.value = false` 移出 finally，避免 `nextTick` 时 chartRef 仍是 null 导致 chart 空白（早期 console 警告 `renderChart skipped: chartRef or payload missing null`）。

**浏览器验证**：事件 `id=4`（外交部回应日自卫队舰艇）→ 30 篇情报 / 4 篇命中 ABSA 缓存 / 覆盖率 13% / 3 个 12h 桶 / top5 aspect（台湾海峡、中日关系、日本首相高市早苗、中国主权、日本自卫队）全部 -1.0（事件本身偏负面，符合预期）。

---

### 2026-05-15 · W8 收口 + J/K + F7

**Commit 链**：`d70e2ca` → `1e4dd89` → `189a555` → `00861c4` → `b4784b2` → `df7d3dd` → `7f55c01`

**交付项**：
- F1 漏洞 1+3+T2 三处加固（搜索语义 JSDoc / `Math.min(99, ...)` 硬上限 / 时序依赖修复）
- Batch VI 侧栏 active 视觉强化
- Batch VII/VIII 卡片 hover 200ms ease-out + MoreHeadlines row 影响指数
- **J/K** 后端 `setdefault("rank", idx)` 兜底（[app/crawler/sources/base.py](app/crawler/sources/base.py)）：彻底修复 baidu/bilibili/toutiao/zhihu 4 源 `rank=99` 占位
- **F7** 影响指数可解释面板（`dataAdapter.explainImpact()` + NewsCard tooltip）
- 文档：[对比分析-鱼皮热点监控.md](docs/对比分析-鱼皮热点监控.md)

**真实生效验证**（DB 查询，rank=999 占位排除）：

| source_id | rank 范围 | rank=99 占比 |
|---|---|---|
| baidu_hot / bilibili_hot_video / toutiao_hot / zhihu_hot_question | 1-30 | **0** ✅ |
| weibo_hot_search | 1-30 | 0 |
| thepaper_hot / wallstreetcn_news / cls_telegraph | 1-N | 0 |

**8 榜 F7 tooltip 抽样**：B 站第 1 名「基础分 91 / 播放量 6.46M → +4.3 / 合计 95」；澎湃/华尔街/财联社显示「该平台仅按排名计分，无热度加权」优雅 fallback；rank 1-5 梯度严格匹配 `max(25, round(92-rank*1.4))` = 91/89/88/86/85。

**API 验证**：`GET /api/sync/status` 200；`GET /api/ai/morning_brief/pdf` 200 / 208KB / 2.44s。

**已知约束**（非 bug，留作后续优化）：
1. T1 工科榜分数偏低：头条/百度/微博 `hot_value` 缩放系数 `0.0000002` 偏小，让排名主导；可在 `BONUS_FIELD_MAP` 单独调整
2. 微博 / 知乎 `hot_value=0`：源数据缺字段，tooltip 显示 `→ +0.0` 是正常 fallback
3. B 站 cookie 失效偶发，需手工凭据弹窗刷新


### V2 画像兴趣 tag 升级（embedding 邻近召回） ✅ v1.6 已落地

- **痛点**：旧 `top_tags` 来自 `article.extra_info.category`，>90% 文章只有「综合」，画像 tag 维度形同虚设
- **新做法**：[app/services/profile_inference.py](app/services/profile_inference.py)
  - 取最近 50 条 view_history（action 加权：view=1 / open=2 / dwell=clamp(ms/5000, 1, 5)）
  - 加权聚合 BGE article embedding → L2 归一为「用户兴趣向量」centroid
  - 用 centroid 邻近召回最近 7 天 cos≥0.55 的 Top80 篇文章，join `Event.keywords` 抽取真实关键词
  - 累计权重排序得 Top12 inferred_tags，30 min TTL
- **实测**（78 view_history → 26 篇 → 1690 候选 → 80 命中）：
  特朗普(5.95) / 美国(5.32) / ai(4.69) / 中国(4.61) / 母亲节(3.99) / 总统(3.26)
- **冷启动**：`MIN_HISTORY_FOR_INFER=5`，浏览不足 5 篇时返回空数组

### A1 推荐解释徽章结构化 ✅ v1.6 已落地

- 后端：`scored` 元组 3→4 元 `(score, ev, reasons, chips)`，响应增加 `_recommend_chips`
- chip 类型 6 类：keyword / sub_source / profile_source / profile_tag / semantic / fallback
- 前端：6 色徽章渲染 + hover tooltip 完整公式（如 `订阅词「美国」字面命中标题 +5`）
- 同时删除冗余「兴趣标签 TOP」block（旧 `top_tags` 只能显示「综合」）

---

### S1（原始规划备份）

- **现状**：`_today_recommendations`（routes.py L3683）的关键词命中是 `kw in title`
  字面子串匹配 —— 订阅"半导体"召不到"芯片产业链"标题，订阅"AI"召不到"人工智能"。
  完整算法见 7 维加权排序：① 订阅 keyword/event 命中 +5×weight ② 订阅 source 命中
  事件主源 +3×weight ③ 画像 source/tag 偏好按比例 0~+3 ④ blocklist 黑词剔除
  ⑤ 同分按事件 heat_score 兜底 ⑥ fallback 模式打 _fallback=true 防空白页 ⑦ RAG
  has_keyword_match 召回门槛同样字面匹配。
- **痛点**：用户需手动列举所有同义词，体感像"古早搜索引擎"，与"AI 智能体舆情系统"
  的产品定位不符。
- **方案**（二选一或叠加）：
  1. **embedding 语义召回**：复用现有 `bge-small-zh-v1.5`（W1 已接入），订阅词
     与候选事件标题/摘要分别向量化，余弦相似度 ≥ 阈值（建议 0.55）即视为命中，
     得分 = `cosine × 5 × weight`。订阅词向量可缓存到 `subscriptions.embedding_blob`
     字段，事件向量已在 `EventEmbedding`（如无则补建）。
  2. **LLM 同义词扩展**：用户配置订阅词时，后端调一次 DeepSeek 让其产出 3-5 个
     同义词/上位词（如"半导体" → ["芯片", "晶圆", "IC", "集成电路"]），存到
     `subscriptions.synonyms` JSON 字段，命中检测时遍历同义词。优点：零延迟
     在线推理；缺点：依赖一次 LLM 调用，需缓存 + 失败兜底。
- **验收标准**：
  - 订阅"半导体"，今日推荐应能召回标题含"芯片/晶圆/IC"的事件
  - `_today_recommendations` 响应增加 `match_type: 'literal' | 'semantic' | 'synonym'`
    字段，前端订阅命中标签可显示"语义命中 0.72"
  - 关闭语义开关时回退到字面匹配（保持向后兼容）
- **工作量**：M（约 2 天，含订阅 CRUD 改造 + 前端命中标签 UI）
- **依赖**：`bge-small-zh-v1.5`（已接入）/ `EventEmbedding` 表 / DeepSeek API
- **风险**：低；语义阈值需小流量灰度调参，过低可能引入噪声推荐
- 一键启动脚本 `scripts/demo.ps1`
- 回归测试：W3 评测集 P/R/F1 不回退，生产 rebuild 5000+ 事件无崩溃

> 关键设计决策（聚类路径 / 情感双轨 / BERT 后台精标 / PDF 字体 / API 配置化 / 云端部署）请查 [docs/项目说明.md](docs/项目说明.md) §8。

## A.9 被砍 / 延后项目

- 公网部署 / 鉴权限流 / HTTPS
- PostgreSQL 迁移
- 移动端 PWA（U3 仍在路线图，未启动）
- 立场分析
- 定时日报邮件推送
- 暗色主题（U1 / D，回退后明确放弃）
