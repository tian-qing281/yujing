# 舆镜 YuJing · 更新内容

> 全部升级与修复的总账本（CHANGELOG），按时间倒序。
>
> - 仅记录用户/评委可感知的能力变化与关键修复，不记录纯文档内部修订
> - 详细未来规划与历史里程碑请查 [升级路线图.md](升级路线图.md)
> - 项目能力总览请查 [docs/项目说明.md](docs/项目说明.md)

---

## v1.5 · F1 时间轴 + P1 增量聚类 + S1 语义召回 + S1.1 全量 centroid 回填（2026-05-12）

### F1 · 事件文章时间轴（EventModal）
- 新增「时间 × 平台」散点图：X=发布时间、Y=源平台、节点尺寸映射 importance_score（11–26 px）、颜色映射情感
- ECharts 6 实现：scatter + dataZoom（inside + slider 紫色调）+ time x-axis + category y-axis
- tooltip 含标题/源/时间/情感色块/摘要前 60 字 + 「点击查看详情 →」一键跳详情
- 文本自动换行（white-space:normal + word-break + overflow-wrap）防止长标题溢出白框
- 显示阈值：`displayedArticles.length >= 3` 时才出现，避免短事件视图冗余

### P1 · 增量事件聚类（高 ROI）
- **能力**：新增 `POST /api/admin/events/incremental`，仅处理 `clustered_at IS NULL` 的最近文章，复用既有 BGE 向量与 FAISS 链路；从「每次清空 events 全量重建」升级为「在线挂载 + 在线均值更新 centroid」
- **DB 改造**：
  - `articles.clustered_at` (DATETIME) — 标记已落库的聚类时间
  - `events.centroid` (BLOB, float32) + `events.centroid_count` (INTEGER) — 在线均值簇心
  - 启动期 `ensure_migrations()` 幂等 ALTER TABLE，旧数据零影响
- **算法**：余弦命中 ≥ 0.69 即挂到既有事件；未命中走 buffer 内 mini Union-Find 形成新事件；挂载后在线更新 `c' = L2((c·n + v)/(n+1))`
- **实测**：首次 200 条冷启动 → 183 个新事件（centroid 全 NULL，全部走新建）；第二批 200 条 → **42 条命中既有 centroid 挂载（21%）**，151 条新事件
- **显著效果**：单批次仅扫描未聚类文章而非全库，避免每次清空 events 表；scheduler 仍保留 `rebuild_events` 全量做兜底，不影响现网

### S1 · 订阅语义召回（升级）
- **能力**：`/api/recommendations` 默认开启语义召回（`use_semantic=true / w_semantic=6.0 / semantic_threshold=0.40`），订阅词不再仅靠 `kw in title` 字面匹配
- **实现**：新建 [app/services/subscription_semantic.py](app/services/subscription_semantic.py)，复用 P1 写入的 `Event.centroid`（BGE-small-zh L2 归一）；订阅词通过 `_get_sub_vector` 现编码 + 进程内缓存（cache key 含模型名，换模型不命中错向量）
- **算法**：`sub_mat (S, dim) @ event.centroid` 取 argmax，`cosine ≥ threshold` 才记录；语义命中分 = `cosine × w_semantic × weight`，与字面 `kw in title` 同位合并到 `sub_weight_map`；去重保护：仅当订阅词不出现在标题中时才叠加（避免双重加权）
- **响应字段**：新增 `semantic_used / semantic_hits / weights.semantic / semantic_threshold`；`_recommend_reasons` 直显「语义相似「人工智能」 0.42」
- **显著效果**：订阅「房地产」可召回标题「未来房子只有两种」(0.42)，订阅「美国」可召回「中美关系发展方向」(0.48) 与「中美元首北京会晤」(0.43) — 这些在字面匹配下完全不会进入推荐列表
- **MVP 边界**：暂不动 `subscriptions` schema、不动前端 UI；后续可补订阅词向量持久化与命中标签 UI
- **阈值/权重依据**（[scripts/eval_s1_threshold.py](scripts/eval_s1_threshold.py) 实测）：
  - 10 个订阅词 × 334 事件扫阈值 0.30~0.65；cos 全局分布集中在 0.30~0.40（3±4 字订阅词与长标题 mean-pool 后的自然中心带）
  - 阈值=0.40：假阳率 ~30%（如「房地产 ← 中芯国际」 0.50、「气候变化 ← 轮子进化」 0.42）
  - 阈值=0.45：假阳明显下降，如「房地产 ← 广州楼市新政首个日光盘」 0.46、「中美元首」 0.43，带意义命中为主 → 选作默认
  - 权重 w_semantic=6.0：cos=0.50 加分 = 3.0；字面命中 5.0。语义不超过字面，不喧宾夺主

### S1.1 · 全量 events.centroid 回填
- **背景**：P1 增量聚类仅处理新文章，存量 6880 个老事件仍无 centroid（覆盖仅 4.6%），S1 语义召回受资料面限制
- **实现**：[scripts/backfill_event_centroid.py](scripts/backfill_event_centroid.py) 一次性脚本，复用已有 `article_embeddings`（W1 阶段铺好的10474 条 BGE 向量），不走 GPU 推理： `centroid = L2_normalize(mean(article_vectors))` 与 P1 算法一致
- **性能**：6880 个事件 0.4 秒全部回填（17000+ ev/s）
- **覆盖**：334/7214 (4.6%) → **7214/7214 (100%)**
- **语义命中提升**：同一 limit=200 查询下，命中 5 → 7；特别是 Top10 出现「特朗普：中国是一个令人惊叹的国家」(0.54)、「中美共同的两个敌人」(0.51)、「一线城市二手房成交回暖」(0.51) 这类高质量纯语义命中

---

## v1.4 · 二期产品质感打磨（2026-05-12）

### 文档治理（大整理）
- 合并《项目总览文档》《系统设计文档》→ [docs/项目说明.md](docs/项目说明.md)（十节，含 CHANGELOG）
- 核心原理文档瘦身：聚焦评分体系/排序策略/检索精排 6 块独家算法
- 新增 [docs/ABSA算法文档.md](docs/ABSA算法文档.md) 独立专题（prompt 工程 + 文件缓存 + 性能）
- 升级路线图迁至根目录并改中文名 [升级路线图.md](升级路线图.md)；删除 W3/W4 评测数据冗余（已在评测文档）
- 各专题文档头部加上下文跳转区
- 删除冗余文件：CLAUDE.md / docs/项目总览文档.md / docs/系统设计文档.md / 项目规范.txt
- 答辩准备清单修正 3 处过时（版本日 / 任务集 50→40 / 数据库路径 `app→runtime`）

### UI / 体验修复
- ABSA 卡限高 460→300，与左侧情感极性图等高对齐
- 订阅页翻页按钮加紫色渐变背景；忽略 X 加白底框；箭头 #cbd5e1→#64748b 提高对比度
- BriefModal 早报独立弹窗，与异动推送解耦
- AnalysisModal 新增 B 站评论 / 弹幕 双情绪卡（双 BERT 引擎，处理 200+ 评论与弹幕）
- 推荐打分 4 维权重透明化：UI 滑杆可调，本地持久化

### 后端能力
- ABSA 文件缓存（sha1）落盘 `runtime/absa_cache/`，多次命中 < 5ms
- ABSA 12s 软超时（asyncio.wait_for），失败不阻塞主分析链路
- 订阅推荐 4 维加权（订阅词/订阅源/画像源/画像兴趣）+ fallback 兜底
- 用户画像（常看源 TOP / 兴趣标签 TOP）反哺推荐打分
- 事件 bucket_hours 时间桶（小时粒度情绪曲线）

---

## v1.3 · W5 收官补丁（2026-05-09）

### Tool-Calling Agent 打磨
- 第 10 工具 `compare_platforms`：封装 `/api/ai/compare`，前端对话统一走 Agent
- AgentTrace 后接 CompareDashboard 形成 "思考 → 数据 → 结论" 阅读流
- 流式打字机修复：`final_delta` + 30ms 分帧
- LLM 思考过程 SSE 流式：`llm_thinking / llm_thinking_delta / llm_done`
- 评测指标：100% 完成率 / 平均延迟 26s（W4 37s）/ 平均步数 3.5（W4 4.9）

### 全链路稳定化
- 事件排序 `id DESC` tiebreaker 全链路修复（解决 Top-9 集合在同分事件间漂移）
- 前端 `compareEvents` 改分级整数比较（避免浮点 ±ε 引发 sort 抖动）
- `scheduleEventHubHydration` 不再调 `fetchEvents`，统一走 `fetchUnifiedSearch` 单一写入通道
- AbortController + requestId 并发保护

### 性能优化
- charts-vendor 首屏拆分：1093KB → 543KB（−50%）
- Agent 工具并发化（`asyncio.gather`）：37s → 26s
- BERT batch=8（`emotion.analyze_batch()`）
- MeiliSearch `typoTolerance` 400 修复 + 启动自动 build

### 5 轮前端 Bug 修复
- 用户消息不显示 / LLM 流式 / 圆圈旋转 / 热榜空白 / 统一 Agent 模式
- Agent 预设条覆盖全屏修复 / OMP 重复库崩溃修复

---

## v1.2 · W5 收尾（2026-04 末）

- 多轮对话 12 历史窗口
- ZH 长标题对比 grid 布局修复
- `/api/articles/{id}` 轻量详情接口（Agent 引用未命中内存时 fallback）
- 引用 Badge（TopicModal 文字+图标 / AIConsultant 仅图标）
- PDF 字体修复：`msyh.ttc`（微软雅黑）+ 副标题恢复
- 前端 API 配置化（26 处硬编码 → `buildApiUrl()` + `VITE_API_BASE_URL`）

---

## v1.1 · W4 智能体上线（2026-04 中）

- Tool-Calling Agent 引擎落地（约 270 行核心代码，11 单测离线 FakeLLM 注入）
- 首批 9 注册工具：search_events / get_event_detail / search_articles / semantic_search_articles / analyze_event_sentiment / compare_events / get_morning_brief / list_hot_platforms / rank_events_by_sentiment
- 20 query 评测集冻结：完成率 95% / 平均延迟 37s / 平均步数 4.9
- AgentTrace 可视化面板（思考链时间线）
- 双引擎情感：EmotionEngine（8 类 Chinese-Emotion）+ SentimentEngine（c3-v2 三分类）

---

## v1.0 · W3 语义聚类正式接入（2026-04 上）

### 论文级评测冻结
- BGE-small-zh + FAISS IVF-Flat + 双层 Otsu 自适应阈值
- 主表（91 pair + 155 闭集 gold · seed-100）：F1 **0.827** vs Jaccard 0.471（+76%）
- ARI 0.691（+127%）/ Recall 0.795（×2.58）/ NMI 0.977
- 消融实验：fixed_threshold F1 −0.027；disable_canonical_merge F1 −0.060
- canonical rescue 修复 + 标注仲裁闭环

### 早期能力（W1-W2）
- BGE-small-zh 嵌入接入 + ArticleEmbedding 表 + 批量物化
- `cluster_articles_semantic_faiss` + 双层 Otsu + canonical 硬合并边
- Jaccard 聚类作 fallback（IDF 加权 + canonical 加分）

---

## 数据采集（持续迭代）

- 10 平台稳定接入：微博 / 百度 / 头条 / 知乎 / B 站 / 财联社 / 华尔街见闻 / 澎湃 / 36 氪 / 少数派
- APScheduler 120s 周期；Cookie 凭据失效友好提示卡片
- Jina Reader 正文抽取（针对头条/B 站/知乎专用解析）
- B 站评论 / 弹幕：buvid3 cookie 预热修复 reply -352 风控；cursor 翻页；50 BV 本地兜底数据集（4.7MB / 100 文件）

---

## 被砍 / 已放弃项

- 暗色主题（U1 / D，全组件适配成本高，评估后回退）
- 公网部署 / 鉴权限流 / HTTPS（不在答辩范围）
- PostgreSQL 迁移 / Docker 容器化（云部署原则：直接 `python -m app.main` 一条命令全启动）
- 立场分析 / 定时日报邮件推送（延后到二期之后）

---

> 完整提交历史请用 `git log --oneline`；本表只摘录有外部价值的变更。
