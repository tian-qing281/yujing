# 舆镜 YuJing · 更新内容

> 全部升级与修复的总账本（CHANGELOG），按时间倒序。
>
> - 仅记录用户/评委可感知的能力变化与关键修复，不记录纯文档内部修订
> - 详细未来规划与历史里程碑请查 [升级路线图.md](升级路线图.md)
> - 项目能力总览请查 [docs/项目说明.md](docs/项目说明.md)

---

## v1.9 · F5 N 平台舆情雷达图 + L2-L5 体验收尾（2026-05-16）

### F5 · 跨平台 N 平台舆情画像雷达图
- **能力**：自然语言"画一张微博/知乎/头条/百度/B站的舆情雷达图"→ Agent 自动调 `compare_platforms_radar` 工具 → 前端 ECharts 5 边形雷达图实时渲染
- **后端**：[app/services/agent/tools/tool_compare_platforms_radar.py](app/services/agent/tools/tool_compare_platforms_radar.py) 新工具，5 维归一化到 0~100：
  - **情报量** log10 归一（FULL=5000 篇 ≈ 满分）
  - **事件覆盖** events / 8 × 100
  - **情绪鲜明度** (positive+negative+surprise) / sample × 25 倍率（兼顾稀疏标注）
  - **24h 时效** c_0_24 / sample × 100
  - **单事件规模** log10(articles/events + 1) / log10(2001) × 100
  - 输入 platforms 数组（2~6 个）+ 可选 topic 过滤；输出含 `dimensions` / `platforms[].scores` / `leaders[]` 各维度领跑者
- **前端**：[CompareDashboard.vue](yujing-ui/src/components/CompareDashboard.vue) 新增 `radar-mode` 分支：
  - flex column 布局（雷达图 + 双列底部面板：领跑者 + 原始数据表）
  - 5 平台调色板（红/蓝/绿/紫/黄）+ 多边形 area 半透明填充
  - chartInst dispose 重建 + ResizeObserver 兜底，解决 v-if 切换 / 父气泡布局异步导致的"canvas 被压扁"问题
- **AI Consultant**：[AIConsultant.vue](yujing-ui/src/components/AIConsultant.vue) SSE 解析识别 `_type=platform_radar` 输出，写入 `compare_metrics`；`TOOL_FOLLOW_UPS` 加追问引导
- **真实数据样例**（5 平台快照）：
  | 平台 | 情报量 | 事件覆盖 | 情绪鲜明度 | 24h时效 | 单事件规模 |
  |---|---|---|---|---|---|
  | 微博 | 94.9 | **100** | 0 | 45 | 79 |
  | B 站 | 81.9 | 100 | 0 | 39 | 64.5 |
  | 头条 | 93 | 0 | 0 | **48.7** | 0 |
  | 百度 | 87.5 | 62.5 | **50** | 29.7 | 76.9 |
  | 知乎 | 85.3 | 37.5 | 0 | 37.7 | **81.1** |
  - 5 维分别由 4 个不同平台领跑 → 雷达图自然呈现"各平台舆情画像差异化"叙事
- **答辩价值**：从"两两 compare_platforms"升级为"N 平台一图统观"，自然语言一句话即可生成；评委可直观看到"微博走广度 / 知乎走深度 / 百度情绪标注最完整 / 头条纯热点流"

### L2 · scheduler 增量聚类已就绪
- 已确认：[app/crawler/scheduler.py](app/crawler/scheduler.py) 定时器为 `_run_incremental_cluster_job`（每 10min 增量）+ `_run_centroid_calibrate_job`（每 6h centroid 校准），全量 rebuild 仅在 SWR 用户触发路径保留作为权威重算

### L3 · 早报导出失败 toast 提示
- [AIConsultant.vue](yujing-ui/src/components/AIConsultant.vue) `downloadBlobAs` 改为 throw error；自实现轻量 `showToast(text, kind)`（无需引入组件库）；早报失败时根据状态码区分"早报正在生成中" / 通用错误，避免静默失败

### L4 · favicon + meta 完善
- [yujing-ui/index.html](yujing-ui/index.html) 补 `meta description` / `keywords` / `og:title` / `og:description` / `theme-color`；title 升级为"舆镜 YuJing · 全网舆情聚合分析"；favicon 沿用既有 SVG

### L5 · export_pdf POST size 防御
- [routes.py](app/api/routes.py) `/ai/export_pdf` 加请求体限制：content ≤ 200KB、images ≤ 8 张、单图 ≤ 4MB；超限返回 413 + 明确 detail，避免恶意 payload 击穿 PDF 渲染

---



### F4 · Word/PPT 导出（复用 PDF 模板）
- **能力**：4 个原 PDF 端点（文章详情 / 事件详情 / 早报 / Agent 对话）统一加 `?format=pdf|docx|pptx` query，三格式共用同一份 sections 数据结构
- **后端**：[app/services/report_export.py](app/services/report_export.py) 新增 `build_docx` / `build_pptx`：
  - DOCX：标题 Pt(20) 居中、章节 13pt 粗体、行内 `**xxx**` 解析为 bold run、嵌图自动按宽缩放
  - PPTX：16:9 宽屏（13.333×7.5 英寸），封面页 + 截图页 + 各 section 一页
  - `MEDIA_TYPES` + `normalize_format(fmt)` 兜底未知格式降级 pdf
- **前端**：[AIConsultant.vue](yujing-ui/src/components/AIConsultant.vue) 早报 banner + 消息卡片 + [BriefModal.vue](yujing-ui/src/components/BriefModal.vue) 详情页，「导出 PDF」单按钮 → 「PDF / Word / PPT」三按钮组
- **依赖**：python-docx、python-pptx 1.0.2、Pillow（已写入 requirements.txt）
- **答辩价值**：评委可现场 1 秒下载 .docx 二次编辑、.pptx 直接演示，从「单一交付」升级为「三格式选择」

### F3 · 多轮记忆复盘确认 ✅
- 路线图 F3 节标 ✅ 已交付：AIConsultant 已实现 sessions 多会话 + localStorage 持久化 + `req.history[-4:]` 后端注入，单机演示场景已 100% 覆盖
- 跨设备同步留作 V3 路线图（需引入用户体系，本期不投入）

---



### F7 · 影响指数算法可解释面板（强答辩点）
- **能力**：[yujing-ui/src/utils/dataAdapter.js](yujing-ui/src/utils/dataAdapter.js) 新增 `explainImpact(article)`，输出 4 行人话拆解：`平台·排名 / 基础分 / 平台热度加权 / 合计`
- **实现**：每平台独立 `BONUS_FIELD_MAP`（B 站取 view、微博取 reads、知乎取 followers），`baseScore = max(25, round(92 - rank × 1.4))`；hover NewsCard heat badge 与 MoreHeadlines row 数字均显示完整公式
- **抽样验证**（2026-05-15 真实生效）：
  - B 站第 1 名：`基础分 91 / 播放量 6.46M → +4.3 / 合计 95`
  - 微博第 1 名：`基础分 91 / 阅读量 0 → +0.0 / 合计 91/99`（fallback 文案"热度值 0"）
  - 澎湃 / 华尔街见闻 / 财联社：`该平台仅按排名计分，无热度加权`（优雅 fallback）
- **意义**：解决"为什么这条 95 分另一条 89 分"的可信度黑盒，鱼皮 yupi-hot-monitor 无此类透明度设计

### J/K · 全平台 rank=99 占位彻底修复
- **问题**：baidu/bilibili/toutiao/zhihu 4 个 source 在 `_save_to_db` 入口未塞 `rank`，导致 DB 落库时全部为 99 占位，前端基础分恒等于 `max(25, 92-99×1.4) = 25`
- **修复**：[app/crawler/sources/base.py](app/crawler/sources/base.py) `_save_to_db` 入口 `enumerate` 注入 `data.setdefault("rank", idx + 1)`，覆盖所有未显式塞 rank 的 source
- **验证**（DB 直查，rank=999 是过期占位排除）：
  | source_id | rank 范围 | rank=99 占比 |
  |---|---|---|
  | baidu_hot / bilibili_hot_video / toutiao_hot / zhihu_hot_question | 1-30 | **0** ✅ |
  | weibo_hot_search / thepaper_hot / wallstreetcn_news / cls_telegraph | 1-N | 0 |
- **前端**：`recomputeImpactByGroup` 保留作为防御层 + 搜索页相关度近似，双重兜底

### Batch VI-X · UI 一体化打磨
- **Batch VI 侧栏 active 视觉强化**：当前选中项底色加深 + 左边 3px coral 竖条，避免视觉迷失
- **Batch VII/VIII 卡片 hover 200ms ease-out**：所有 NewsCard 与 MoreHeadlines row 统一 transition；MoreHeadlines row 新增右侧影响指数数字（hover 显示 F7 tooltip）
- **F1 漏洞 1+3+T2 三处加固**：搜索语义 JSDoc 注释 / `Math.min(99, ...)` 影响分硬上限 / EventModal 时序依赖修复（先 setEventArticles 再触发 ECharts）

### 文档治理
- 删除 `docs/今日交付总结-2026-05-15.md` + `docs/全功能验证报告-2026-05-15.md`（冗余日总结）
- 增量收口至 [升级路线图.md](升级路线图.md) 末尾「最近交付节点（追加式日志）」节，今后所有交付节点统一追加，不再新建独立 `YYYY-MM-DD-总结.md`
- 新增 [docs/对比分析-鱼皮热点监控.md](docs/对比分析-鱼皮热点监控.md)：与 liyupi/yupi-hot-monitor 五维差异分析 + 答辩话术建议

### 已知约束（非 bug，留作后续）
1. T1 工科榜分数偏低：头条/百度/微博 `hot_value` 缩放系数 `0.0000002` 偏小，让排名主导
2. 微博 / 知乎 `hot_value=0`：源数据缺字段，tooltip "热度值 0 → +0.0" 是正常 fallback
3. B 站 cookie 失效偶发，需手工凭据弹窗刷新

---

## v1.6 · S1.2 订阅词向量持久化 + V2 画像兴趣推断 + A1 推荐解释徽章（2026-05-12）

### S1.2 · 订阅词向量持久化 + 语义命中徽章
- **能力**：创建订阅时立即编码并落库，避免每次推荐重复跑 BGE
- **DB 改造**：`subscriptions.embedding` (BLOB, float32, 512 dim, L2 归一)，`ensure_migrations()` 幂等 ALTER
- **实现**：[app/services/subscription_semantic.py](app/services/subscription_semantic.py)
  - `encode_and_persist_subscription(db, sub)`：仅对 `kind ∈ {keyword, event}` 编码并写库
  - `prime_cache_from_db(db, subs)`：推荐入口将 DB 向量灌进进程缓存，命中即取，未命中 lazy 现编码
- **前端徽章**：订阅推荐项 reasons 新增小号青绿徽章 `语义命中 0.54`，与字面命中、画像命中并列展示（A1 已重做为 6 色徽章体系）

### V2 · 画像兴趣 tag 升级（embedding 邻近召回）
- **背景**：旧 `top_tags` 来自 `article.extra_info.category` 弱信号，绝大多数文章只有「综合」一项，画像维度形同虚设
- **新做法**：[app/services/profile_inference.py](app/services/profile_inference.py)
  - 取最近 50 条 view_history（action 加权：view=1 / open=2 / dwell=clamp(ms/5000, 1, 5)）
  - 加权聚合用户已读文章的 BGE embedding → L2 归一为「**用户兴趣向量** centroid」
  - 用 centroid 邻近召回最近 7 天 Top80 篇文章（cos ≥ 0.55），join `EventArticle → Event.keywords` 抽取真实关键词
  - 累计权重 → 推断 Top12 兴趣 tag，30 min TTL 缓存
- **实测**（78 条 view_history → 26 篇有效文章 → 1690 邻近候选 → 80 命中）：
  Top tag = 特朗普(5.95) / 美国(5.32) / ai(4.69) / 中国(4.61) / 母亲节(3.99) / 总统(3.26)
- **响应字段**：`/api/profile` 新增 `inferred_tags` (top12) + `inferred_meta`（采样数 / 候选数 / 截断时间），同步落 `UserProfile.data`
- **前端**：画像面板新增「**推断兴趣**」chips（青绿 `sub-tag--inferred`），与字面 tag 颜色区分；浏览满 5 篇即可生成（`MIN_HISTORY_FOR_INFER=5`）

### A1 · 推荐解释徽章结构化 + 6 色区分
- **背景**：旧 `_recommend_reasons` 是中文字符串数组，前端只能整段渲染，颜色单一、信息密度低
- **后端**（[app/api/routes.py](app/api/routes.py) `/api/recommendations`）：
  - `scored` 元组由 3 元升级为 4 元 `(score, ev, reasons, chips)`，响应新增 `_recommend_chips` 字段（取前 5）
  - `chip` 结构：`{type, text, weight?, score?}`
  - `type` 枚举：`keyword / sub_source / profile_source / profile_tag / semantic / fallback`
  - `profile_source` 仅当 `inc ≥ 0.3` 才生成 chip，避免低权重噪声刷屏
  - 旧 `_recommend_reasons` 字段保留作前端兜底
- **前端**（[yujing-ui/src/components/SubscriptionPanel.vue](yujing-ui/src/components/SubscriptionPanel.vue)）：
  - 6 色徽章：订阅词=蓝 / 订阅源=紫 / 常看=橙 / 兴趣=粉 / 语义=青绿渐变 / 兜底=灰
  - hover tooltip 显示完整公式（如 `订阅词「美国」字面命中标题 +5`）
  - 删除旧「兴趣标签 TOP」block（只能显示「综合」无意义），只保留 V2 推断的真实兴趣
- **常见疑问 FAQ**：
  - Q：为什么「常看 baidu_hot +2」会出现 +2、+3 这种值？
  - A：公式 `inc = w_profile_source(=3) × (你点该源次数 / 你最常看那个源的次数)`；点最多的源 → +3，第二常看的源 → +2，偶尔点的 → +0.5；是相对权重不是绝对次数

---

## v1.5 · F1 时间轴 + P1 增量聚类 + S1 语义召回 + S1.1 全量 centroid 回填 + P1.1 scheduler 上线（2026-05-12）

### F1 · 事件文章时间轴（EventModal）
- 新增「时间 × 平台」散点图：X=发布时间、Y=源平台、节点尺寸映射 importance_score（11–26 px）、颜色映射情感
- ECharts 6 实现：scatter + dataZoom（inside + slider 紫色调）+ time x-axis + category y-axis
- tooltip 含标题/源/时间/情感色块/摘要前 60 字 + 「点击查看详情 →」一键跳详情
- 文本自动换行（white-space:normal + word-break + overflow-wrap）防止长标题溢出白框
- 显示阈值：`displayedArticles.length >= 3` 时才出现，避免短事件视图冗余

### P1 · 增量事件聚类（高 ROI）
- **能力**：新增 `POST /api/admin/events/incremental`，仅处理 `clustered_at IS NULL` 的最近文章，复用既有 BGE 向量与 FAISS 链路；从「每次清空 events 全量重建」升级为「在线挂载 + 在线均值更新 centroid」
- **DB 改造**：
  - `articles.clustered_at` (DATETIME) — 标记已落库的聚类时间
  - `events.centroid` (BLOB, float32) + `events.centroid_count` (INTEGER) — 在线均值簇心
  - 启动期 `ensure_migrations()` 幂等 ALTER TABLE，旧数据零影响
- **算法**：余弦命中 ≥ 0.69 即挂到既有事件；未命中走 buffer 内 mini Union-Find 形成新事件；挂载后在线更新 `c' = L2((c·n + v)/(n+1))`
- **实测**：首次 200 条冷启动 → 183 个新事件（centroid 全 NULL，全部走新建）；第二批 200 条 → **42 条命中既有 centroid 挂载（21%）**，151 条新事件
- **显著效果**：单批次仅扫描未聚类文章而非全库，避免每次清空 events 表；scheduler 仍保留 `rebuild_events` 全量做兜底，不影响现网

### S1 · 订阅语义召回（升级）
- **能力**：`/api/recommendations` 默认开启语义召回（`use_semantic=true / w_semantic=6.0 / semantic_threshold=0.40`），订阅词不再仅靠 `kw in title` 字面匹配
- **实现**：新建 [app/services/subscription_semantic.py](app/services/subscription_semantic.py)，复用 P1 写入的 `Event.centroid`（BGE-small-zh L2 归一）；订阅词通过 `_get_sub_vector` 现编码 + 进程内缓存（cache key 含模型名，换模型不命中错向量）
- **算法**：`sub_mat (S, dim) @ event.centroid` 取 argmax，`cosine ≥ threshold` 才记录；语义命中分 = `cosine × w_semantic × weight`，与字面 `kw in title` 同位合并到 `sub_weight_map`；去重保护：仅当订阅词不出现在标题中时才叠加（避免双重加权）
- **响应字段**：新增 `semantic_used / semantic_hits / weights.semantic / semantic_threshold`；`_recommend_reasons` 直显「语义相似「人工智能」 0.42」
- **显著效果**：订阅「房地产」可召回标题「未来房子只有两种」(0.42)，订阅「美国」可召回「中美关系发展方向」(0.48) 与「中美元首北京会晤」(0.43) — 这些在字面匹配下完全不会进入推荐列表
- **MVP 边界**：暂不动 `subscriptions` schema、不动前端 UI；后续可补订阅词向量持久化与命中标签 UI
- **阈值/权重依据**（[scripts/eval_s1_threshold.py](scripts/eval_s1_threshold.py) 实测）：
  - 10 个订阅词 × 334 事件扫阈值 0.30~0.65；cos 全局分布集中在 0.30~0.40（3±4 字订阅词与长标题 mean-pool 后的自然中心带）
  - 阈值=0.40：假阳率 ~30%（如「房地产 ← 中芯国际」 0.50、「气候变化 ← 轮子进化」 0.42）
  - 阈值=0.45：假阳明显下降，如「房地产 ← 广州楼市新政首个日光盘」 0.46、「中美元首」 0.43，带意义命中为主 → 选作默认
  - 权重 w_semantic=6.0：cos=0.50 加分 = 3.0；字面命中 5.0。语义不超过字面，不喧宾夺主

### S1.1 · 全量 events.centroid 回填
- **背景**：P1 增量聚类仅处理新文章，存量 6880 个老事件仍无 centroid（覆盖仅 4.6%），S1 语义召回受资料面限制
- **实现**：[scripts/backfill_event_centroid.py](scripts/backfill_event_centroid.py) 一次性脚本，复用已有 `article_embeddings`（W1 阶段铺好的10474 条 BGE 向量），不走 GPU 推理： `centroid = L2_normalize(mean(article_vectors))` 与 P1 算法一致
- **性能**：6880 个事件 0.4 秒全部回填（17000+ ev/s）
- **覆盖**：334/7214 (4.6%) → **7214/7214 (100%)**
- **语义命中提升**：同一 limit=200 查询下，命中 5 → 7；特别是 Top10 出现「特朗普：中国是一个令人惊叹的国家」(0.54)、「中美共同的两个敌人」(0.51)、「一线城市二手房成交回暖」(0.51) 这类高质量纯语义命中

### P1.1 · scheduler 增量聚类 + centroid 定时校准
- **背景**：P1 增量聚类之前仅带手动 admin 入口、未进定时；scheduler 原本完全不跑事件聚类（`_refresh_events_cache` 是死代码）
- **实现**：[app/crawler/scheduler.py](app/crawler/scheduler.py) 注册两个新 job：
  - **增量聚类**：默认每 10 分钟（`INCR_CLUSTER_INTERVAL_MIN`）调 `incremental_cluster(db)`，仅扫 `clustered_at IS NULL` 的近期文章
  - **centroid 校准**：默认每 6 小时（`CENTROID_CALIBRATE_HOURS`）调 `calibrate_event_centroids(db)`，从原始 `article_embeddings` 重算修正在线均值漂移
- **新函数**：`incremental_cluster.calibrate_event_centroids(db, only_missing=False)` — 与 backfill 脚本同逻辑但可选只补空
- **实测**（将间隔调为 2 分钟验证）：`[增量聚类] pending=500 attached=492 new=8` — **复用率 98.4%**（背后是 S1.1 取得 100% centroid 覆盖之后的骨牌效果）
- **可调参数**：`INCR_CLUSTER_INTERVAL_MIN=10` 、 `CENTROID_CALIBRATE_HOURS=6`，设 0 可关闭某个 job

---

## v1.4 · 二期产品质感打磨（2026-05-12）

### 文档治理（大整理）
- 合并《项目总览文档》《系统设计文档》→ [docs/项目说明.md](docs/项目说明.md)（十节，含 CHANGELOG）
- 核心原理文档瘦身：聚焦评分体系/排序策略/检索精排 6 块独家算法
- 新增 [docs/ABSA算法文档.md](docs/ABSA算法文档.md) 独立专题（prompt 工程 + 文件缓存 + 性能）
- 升级路线图迁至根目录并改中文名 [升级路线图.md](升级路线图.md)；删除 W3/W4 评测数据冗余（已在评测文档）
- 各专题文档头部加上下文跳转区
- 删除冗余文件：CLAUDE.md / docs/项目总览文档.md / docs/系统设计文档.md / 项目规范.txt
- 答辩准备清单修正 3 处过时（版本日 / 任务集 50→40 / 数据库路径 `app→runtime`）

### UI / 体验修复
- ABSA 卡限高 460→300，与左侧情感极性图等高对齐
- 订阅页翻页按钮加紫色渐变背景；忽略 X 加白底框；箭头 #cbd5e1→#64748b 提高对比度
- BriefModal 早报独立弹窗，与异动推送解耦
- AnalysisModal 新增 B 站评论 / 弹幕 双情绪卡（双 BERT 引擎，处理 200+ 评论与弹幕）
- 推荐打分 4 维权重透明化：UI 滑杆可调，本地持久化

### 后端能力
- ABSA 文件缓存（sha1）落盘 `runtime/absa_cache/`，多次命中 < 5ms
- ABSA 12s 软超时（asyncio.wait_for），失败不阻塞主分析链路
- 订阅推荐 4 维加权（订阅词/订阅源/画像源/画像兴趣）+ fallback 兜底
- 用户画像（常看源 TOP / 兴趣标签 TOP）反哺推荐打分
- 事件 bucket_hours 时间桶（小时粒度情绪曲线）

---

## v1.3 · W5 收官补丁（2026-05-09）

### Tool-Calling Agent 打磨
- 第 10 工具 `compare_platforms`：封装 `/api/ai/compare`，前端对话统一走 Agent
- AgentTrace 后接 CompareDashboard 形成 "思考 → 数据 → 结论" 阅读流
- 流式打字机修复：`final_delta` + 30ms 分帧
- LLM 思考过程 SSE 流式：`llm_thinking / llm_thinking_delta / llm_done`
- 评测指标：100% 完成率 / 平均延迟 26s（W4 37s）/ 平均步数 3.5（W4 4.9）

### 全链路稳定化
- 事件排序 `id DESC` tiebreaker 全链路修复（解决 Top-9 集合在同分事件间漂移）
- 前端 `compareEvents` 改分级整数比较（避免浮点 ±ε 引发 sort 抖动）
- `scheduleEventHubHydration` 不再调 `fetchEvents`，统一走 `fetchUnifiedSearch` 单一写入通道
- AbortController + requestId 并发保护

### 性能优化
- charts-vendor 首屏拆分：1093KB → 543KB（−50%）
- Agent 工具并发化（`asyncio.gather`）：37s → 26s
- BERT batch=8（`emotion.analyze_batch()`）
- MeiliSearch `typoTolerance` 400 修复 + 启动自动 build

### 5 轮前端 Bug 修复
- 用户消息不显示 / LLM 流式 / 圆圈旋转 / 热榜空白 / 统一 Agent 模式
- Agent 预设条覆盖全屏修复 / OMP 重复库崩溃修复

---

## v1.2 · W5 收尾（2026-04 末）

- 多轮对话 12 历史窗口
- ZH 长标题对比 grid 布局修复
- `/api/articles/{id}` 轻量详情接口（Agent 引用未命中内存时 fallback）
- 引用 Badge（TopicModal 文字+图标 / AIConsultant 仅图标）
- PDF 字体修复：`msyh.ttc`（微软雅黑）+ 副标题恢复
- 前端 API 配置化（26 处硬编码 → `buildApiUrl()` + `VITE_API_BASE_URL`）

---

## v1.1 · W4 智能体上线（2026-04 中）

- Tool-Calling Agent 引擎落地（约 270 行核心代码，11 单测离线 FakeLLM 注入）
- 首批 9 注册工具：search_events / get_event_detail / search_articles / semantic_search_articles / analyze_event_sentiment / compare_events / get_morning_brief / list_hot_platforms / rank_events_by_sentiment
- 20 query 评测集冻结：完成率 95% / 平均延迟 37s / 平均步数 4.9
- AgentTrace 可视化面板（思考链时间线）
- 双引擎情感：EmotionEngine（8 类 Chinese-Emotion）+ SentimentEngine（c3-v2 三分类）

---

## v1.0 · W3 语义聚类正式接入（2026-04 上）

### 论文级评测冻结
- BGE-small-zh + FAISS IVF-Flat + 双层 Otsu 自适应阈值
- 主表（91 pair + 155 闭集 gold · seed-100）：F1 **0.827** vs Jaccard 0.471（+76%）
- ARI 0.691（+127%）/ Recall 0.795（×2.58）/ NMI 0.977
- 消融实验：fixed_threshold F1 −0.027；disable_canonical_merge F1 −0.060
- canonical rescue 修复 + 标注仲裁闭环

### 早期能力（W1-W2）
- BGE-small-zh 嵌入接入 + ArticleEmbedding 表 + 批量物化
- `cluster_articles_semantic_faiss` + 双层 Otsu + canonical 硬合并边
- Jaccard 聚类作 fallback（IDF 加权 + canonical 加分）

---

## 数据采集（持续迭代）

- 10 平台稳定接入：微博 / 百度 / 头条 / 知乎 / B 站 / 财联社 / 华尔街见闻 / 澎湃 / 36 氪 / 少数派
- APScheduler 120s 周期；Cookie 凭据失效友好提示卡片
- Jina Reader 正文抽取（针对头条/B 站/知乎专用解析）
- B 站评论 / 弹幕：buvid3 cookie 预热修复 reply -352 风控；cursor 翻页；50 BV 本地兜底数据集（4.7MB / 100 文件）

---

## 被砍 / 已放弃项

- 暗色主题（U1 / D，全组件适配成本高，评估后回退）
- 公网部署 / 鉴权限流 / HTTPS（不在答辩范围）
- PostgreSQL 迁移 / Docker 容器化（云部署原则：直接 `python -m app.main` 一条命令全启动）
- 立场分析 / 定时日报邮件推送（延后到二期之后）

---

> 完整提交历史请用 `git log --oneline`；本表只摘录有外部价值的变更。
